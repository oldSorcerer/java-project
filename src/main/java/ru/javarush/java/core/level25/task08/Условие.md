
### Цифровой Холст: Расширенные Возможности Рисования 🆕

Вы – художник-программист, и ваша задача – создать основу для графического редактора, который работает с различными отрисовываемыми объектами.

Определите интерфейс Renderable с основным методом paint(), который будет отвечать за саму отрисовку.

Однако, современные инструменты требуют больше гибкости! Добавьте в ваш интерфейс Renderable новый default-метод adjustScale(), который позволит изменять размер объекта по умолчанию, например, выводя сообщение "Размер объекта скорректирован по умолчанию.".

А для универсальных операций, не зависящих от конкретного объекта, таких как проверка цветовой палитры, добавьте static-метод validateColorPalette(), который мог бы вывести "Цветовая палитра проверена, всё в порядке!".

В главном методе main создайте простую реализацию Renderable (например, анонимный класс или простой класс Shape), вызовите метод paint(), затем используйте adjustScale() на вашем объекте, и, наконец, вызовите статический validateColorPalette() напрямую из интерфейса, чтобы продемонстрировать все новые возможности.

#### Требования:
1. Необходимо создать интерфейс с именем Renderable.
2. Интерфейс Renderable должен содержать абстрактный метод paint() без параметров.
3. В интерфейсе Renderable должен быть реализован default-метод adjustScale(), который по умолчанию выводит строку "Размер объекта скорректирован по умолчанию.".
4. В интерфейсе Renderable должен быть реализован static-метод validateColorPalette(), который выводит строку "Цветовая палитра проверена, всё в порядке!".
5. В методе main необходимо создать реализацию интерфейса Renderable (например, с помощью отдельного класса или анонимного класса).
6. В методе main на объекте, реализующем Renderable, должен быть вызван метод paint().
7. В методе main на объекте, реализующем Renderable, должен быть вызван метод adjustScale().
8. В методе main должен быть вызван статический метод validateColorPalette() напрямую через Renderable.