
### Цифровой пересчет старых записей 💾

Представьте, что вы — старый, но очень дотошный библиотекарь в огромном цифровом архиве. Ваша задача — вручную пересчитать каждый бит данных в старом, неструктурированном файле под названием "input.txt", чтобы узнать его точный размер.

Это будет очень медленный, но зато предельно точный процесс, ведь вы будете "читать" его байт за байтом с помощью FileInputStream.

Когда утомительный пересчет завершится, программа должна объявить общее количество найденных байтов в формате: "Прочитано байт: X", где X — это то самое, дотошно вычисленное число.

#### Требования:
1. Программа должна открывать файл "input.txt" для чтения, используя класс FileInputStream.
2. Чтение содержимого файла должно происходить по одному байту за раз, без использования буферизации или других ускоряющих техник.
3. Программа должна подсчитывать точное количество байтов, считанных из файла.
4. После завершения чтения программа должна закрывать FileInputStream для освобождения ресурсов.
5. После подсчета программа должна вывести на экран строку "Прочитано байт: X", где X — вычисленное количество байтов.