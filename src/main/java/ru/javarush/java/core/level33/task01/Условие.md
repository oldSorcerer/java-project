
### Подсчёт ежедневной выручки магазина 💰

Представьте, что вы – главный бухгалтер небольшого, но очень популярного интернет-магазина. В конце каждого дня вам приходят отчёты о продажах, представленные в виде списка денежных поступлений от каждой транзакции. Вам срочно нужно узнать общую сумму выручки за день, чтобы свериться с кассой.

Создайте массив целых чисел int[] dailySalesFigures, который будет содержать значения {10, 20, 30, 40, 50}, представляющие суммы отдельных продаж. Ваша задача — использовать магию IntStream из Java, чтобы быстро и эффективно вычислить общую сумму всех этих поступлений. После этого выведите итоговую сумму выручки на экран, чтобы подтвердить, что все цифры сходятся.

#### Требования:
1. В программе должен быть объявлен и инициализирован массив int[] dailySalesFigures со значениями {10, 20, 30, 40, 50}.
2. Для вычисления суммы элементов массива необходимо использовать IntStream, а не ручной перебор или Stream<Integer>.
3. Сумма должна быть получена с помощью метода sum() IntStream-а, а не с помощью циклов или сторонних библиотек.
4. Итоговая сумма выручки должна быть выведена на экран.