
### Ленивый кот и его ужин: симуляция без результата 😼

Представьте, что вы программируете поведение очень ленивого кота. Он думает о том, чтобы пойти поесть, возможно, даже выбирает, что ему хочется (фильтрация) и представляет, как это будет вкусно (преобразование), но пока что не сделал ни единого шага к миске. То есть, все его действия остаются лишь в мыслях.

Создайте воображаемый список чисел от 1 до 5, которые могут представлять собой различные виды еды. С помощью Stream API настройте цепочку "размышлений" кота: сначала "фильтрацию" (кот думает, стоит ли это вообще есть), а затем "преобразование" (он представляет вкус). Внутри каждой из этих операций (filter и map) добавьте вывод в консоль сообщений, например, "Кот размышляет о еде X" или "Кот представляет вкус X".

Однако, самое главное — не добавляйте к этой цепочке никакую терминальную операцию (например, collect, forEach, sum). Как и ленивый кот, ваша программа не должна выполнить никаких реальных действий, а значит, на экране не должно появиться ни одной строчки. Это покажет, как лениво работает Stream API без команды к действию!

#### Требования:
1. В программе должен быть создан список чисел от 1 до 5.
2. Над этим списком необходимо создать Stream-цепочку с использованием Stream API.
3. В цепочке должны быть использованы операции filter и map.
4. Внутри операций filter и map должны быть предусмотрены вызовы System.out.println с соответствующими сообщениями (например, "Кот размышляет о еде X" и "Кот представляет вкус X").
5. В цепочке Stream не должно быть добавлено ни одной терминальной операции (collect, forEach, sum и т.д.).
6. Программа не должна выводить в консоль ни одной строки, то есть на экране не должно появиться никаких сообщений.