
### Организация библиотеки древних языков программирования 📜

Вы – цифровой архивариус в гигантской библиотеке, где хранятся свитки, посвящённые древним и современным языкам программирования. У вас есть новый массив с названиями языков, и вам нужно создать систему, которая позволит эффективно просматривать их, возможно, даже параллельно, распределяя свитки между разными отделами.

Создайте свой собственный Spliterator для массива строк {"Java", "Python", "C++", "Kotlin"}. Ваш Spliterator должен быть умным и поддерживать деление массива на части с помощью реализации метода trySplit(), чтобы имитировать распределение свитков. Затем используйте StreamSupport.stream с вашим кастомным Spliterator, чтобы вывести все названия языков на экран. Порядок вывода не так важен – главное, чтобы все свитки были учтены.

#### Требования:
1. Необходимо создать собственный класс, реализующий интерфейс Spliterator для массива строк.
2. Spliterator должен работать с массивом строк {"Java", "Python", "C++", "Kotlin"}.
3. Метод trySplit() должен быть реализован так, чтобы Spliterator мог делить массив на части для параллельной обработки.
4. Для обхода элементов массива необходимо использовать StreamSupport.stream с созданным кастомным Spliterator.
5. Все элементы массива должны быть выведены на экран с помощью стрима.
6. Порядок вывода элементов не имеет значения, но каждый элемент должен быть выведен ровно один раз.