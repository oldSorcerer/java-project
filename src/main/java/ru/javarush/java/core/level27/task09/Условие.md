
### Подсчёт общей стоимости активов 💰

Вы разрабатываете финансовое приложение, которому нужно уметь подсчитывать общую стоимость различных активов. Эти активы могут быть представлены как целыми числами (например, количество акций), так и дробными (например, цена за единицу акций). Вам нужен универсальный инструмент, который сможет суммировать любые числовые значения.

Реализуйте статический метод с именем sumNumbers, который принимает список любых чисел (List<? extends Number> list). Этот метод должен пройти по всем числам в списке, сложить их и вернуть итоговую сумму в виде double. В основном методе main создайте пример списка, содержащего как целые, так и дробные числа (например, List.of(1, 2.5, 3)), вызовите ваш sumNumbers метод для этого списка и выведите полученный результат на экран, чтобы продемонстрировать его универсальность.

#### Требования:
1. Метод sumNumbers должен принимать список с типом параметра List<? extends Number>.
2. Метод sumNumbers должен проходить по всем элементам переданного списка и складывать их значения.
3. Метод sumNumbers должен возвращать итоговую сумму в виде значения типа double.
4. Метод sumNumbers должен корректно обрабатывать любые объекты, наследующие Number (Integer, Double и др.).
5. В методе main необходимо создать список, содержащий как целые, так и дробные числа, и передать его в sumNumbers.
6. Результат работы метода sumNumbers должен быть выведен на экран для проверки корректности работы.