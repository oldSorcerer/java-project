
### Универсальная система логирования событий 📜

Вы пишете систему логирования, которая должна быть максимально гибкой. В эту систему могут поступать различные типы данных, но сейчас вам нужно обеспечить возможность добавлять конкретные строковые сообщения в общий журнал событий, который может хранить не только строки, но и любые другие объекты.

Реализуйте статический метод с именем addStrings, который принимает два аргумента: универсальный список для логирования, способный принимать строки или более общие типы (List<? super String> list), и список строковых сообщений, которые нужно добавить (List<String> values). Этот метод должен просто добавить все сообщения из списка values в список list.

В основном методе main создайте экземпляр ArrayList<Object>, который будет служить вашим универсальным журналом. Затем создайте List<String> с несколькими тестовыми сообщениями (например, "Система запущена", "Ошибка базы данных", "Операция завершена"). Вызовите метод addStrings, передав ему эти списки, а после этого выведите содержимое вашего ArrayList<Object>, чтобы убедиться, что все строковые сообщения успешно добавлены в общий поток событий.

#### Требования:
1. Необходимо реализовать статический метод addStrings, у которого первый аргумент объявлен как List<? super String>.
2. Метод addStrings должен добавлять все элементы из списка values (List<String>) в список list (List<? super String>) с помощью метода addAll.
3. В качестве универсального журнала событий необходимо использовать коллекцию типа ArrayList<Object>, чтобы продемонстрировать работу wildcard ? super String.
4. В методе main требуется создать ArrayList<Object> для хранения любых объектов и отдельный List<String> с тестовыми строковыми сообщениями.
5. Метод addStrings должен быть вызван из main с передачей универсального списка и списка строковых сообщений.
6. После добавления сообщений необходимо вывести содержимое ArrayList<Object> на экран для проверки успешного добавления строковых сообщений.