
### Управление партиями товаров на складе 📦

Вы отвечаете за учёт товаров на складе. Все товары имеют уникальные числовые идентификаторы. Вам понадобилось создать временный список товаров для текущей отгрузки, а затем удалить из него один товар. Однако очень важно, чтобы изменения, сделанные во временном списке, автоматически отразились и на основном складском реестре, поскольку временный список по сути является "видом" на часть основного.

Создайте NavigableSet<Integer> и добавьте в него идентификаторы товаров: 10, 20, 30, 40, 50. Теперь сформируйте временный список для текущей отгрузки, который включает товары с идентификаторами от 20 (включительно) до 50 (исключительно) с помощью метода subSet. Затем представьте, что товар с идентификатором 30 был повреждён, и его нужно удалить из текущей отгрузки. Удалите это число из полученного подмножества. После всех операций, чтобы убедиться, что связь между списками работает как надо, выведите на экран сначала содержимое временного списка отгрузки, а затем содержимое основного складского реестра.

#### Требования:
1. В программе должен быть создан NavigableSet<Integer>, в который добавляются элементы 10, 20, 30, 40, 50.
2. Временный список для отгрузки должен быть создан с помощью метода subSet, включающего элементы от 20 (включительно) до 50 (исключительно).
3. Из подмножества (временного списка) должен быть удалён элемент с идентификатором 30.
4. Удаление элемента из подмножества должно автоматически отражаться в основном NavigableSet (реестре).
5. После всех операций необходимо вывести на экран содержимое временного списка отгрузки (подмножества), а затем содержимое основного складского реестра (NavigableSet).