
### Подсчёт посетителей на мега-событии: Потокобезопасный счётчик 🏟️

Представьте, что вы организатор грандиозного музыкального фестиваля, и у вас есть пять входных гейтов. На каждом гейте стоит отдельный сотрудник (представьте его как поток), который регистрирует каждого входящего посетителя. Ваша задача — написать программу, которая будет точно подсчитывать общее количество посетителей, даже если все сотрудники будут одновременно добавлять числа.

Создайте класс FestivalGate с полем типа AtomicInteger, инициализированным нулём. Это будет ваш глобальный, абсолютно точный счётчик посетителей. В этом же классе создайте метод incrementVisitorCount(), который безопасно увеличивает число посетителей на единицу. Теперь "наймите" пять сотрудников, каждый из которых будет работать в отдельном потоке. Каждый из этих потоков должен поприветствовать и зарегистрировать ровно 100 посетителей, вызывая метод incrementVisitorCount() 100 раз.

Запустите все пять потоков. Очень важно дождаться, пока последний посетитель пройдёт через последний гейт, то есть дождаться завершения работы всех потоков. После этого выведите итоговое значение глобального счётчика посетителей. Если всё сделано правильно с использованием AtomicInteger, на экране должно быть ровно 500, что подтвердит идеальную синхронизацию и отсутствие потерь при подсчёте!

#### Требования:
1. Необходимо создать класс FestivalGate с приватным полем типа AtomicInteger, инициализированным нулём.
2. В классе FestivalGate должен быть реализован публичный метод incrementVisitorCount(), который увеличивает значение AtomicInteger на единицу с помощью атомарной операции.
3. В основной части программы необходимо создать пять отдельных потоков, каждый из которых вызывает метод incrementVisitorCount() ровно 100 раз.
4. Все пять потоков должны быть запущены и программа должна корректно дождаться завершения их работы (например, с помощью метода join).
5. После завершения всех потоков программа должна получить итоговое значение счётчика из AtomicInteger и вывести его на экран; результат должен быть равен 500.