
### Обновление текущего лидера гильдии: Безопасное обновление объекта с AtomicReference 👑

Вы разрабатываете систему управления гильдией в онлайн-игре, где всегда должен быть один Текущий Лидер Гильдии. Этот статус может быть передан другому игроку, но только при очень строгом условии: новый лидер может занять трон только в том случае, если текущий лидер является именно тем, кого вы ожидаете.

Создайте простой класс GuildMember с одним полем String name и конструктором для его инициализации. Теперь создайте специальную "переменную", AtomicReference<GuildMember>, и присвойте ей начальное значение — объект GuildMember с именем "Alice". Она будет представлять текущего лидера.

Далее, представьте, что игрок "Bob" пытается стать новым лидером. Используйте мощный метод compareAndSet из AtomicReference, чтобы попытаться заменить "Alice" на "Bob", но только в том случае, если текущий лидер действительно является "Alice".

После попытки смены лидера (независимо от того, была она успешной или нет), выведите имя члена гильдии, который в данный момент хранится в AtomicReference. Если операция compareAndSet прошла успешно, на экране должно появиться имя "Bob", подтверждая, что передача лидерства произошла безопасно и корректно, только при соблюдении ожидаемого условия.

#### Требования:
1. Необходимо создать класс GuildMember с единственным полем String name и конструктором для инициализации этого поля.
2. В задаче должна использоваться переменная типа AtomicReference<GuildMember> для хранения текущего лидера гильдии.
3. AtomicReference<GuildMember> должен быть инициализирован объектом GuildMember с именем "Alice".
4. Для смены лидера необходимо использовать метод compareAndSet, чтобы попытаться заменить объект "Alice" на объект "Bob" только в том случае, если текущий лидер действительно "Alice".
5. Перед вызовом compareAndSet необходимо создать объект GuildMember с именем "Bob".
6. После попытки смены лидерства программа должна вывести имя текущего члена гильдии, хранящегося в AtomicReference, независимо от результата операции compareAndSet.