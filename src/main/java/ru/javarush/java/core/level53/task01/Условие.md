
### Передача эстафетной палочки между роботами: Опасность тупика 🤖

Представьте, что вы управляете двумя роботизированными манипуляторами, Роботом Альфа и Роботом Бета, которым для выполнения задачи требуются два уникальных инструмента: Гаечный Ключ и Отвертка. Эти инструменты можно использовать только по одному. Ваша задача — написать программу, которая моделирует их работу.

В начале своего рабочего дня каждый робот пытается взять первый необходимый ему инструмент, а затем, спустя короткую паузу, пытается захватить второй. Создайте два объекта-ресурса, которые будут служить замками для Гаечного Ключа и Отвертки. Затем запустите два потока: один для Робота Альфа и один для Робота Бета.

Робот Альфа всегда сначала берёт Гаечный Ключ, затем Отвертку. Робот Бета, в силу своей уникальной специализации, всегда сначала берёт Отвертку, а затем Гаечный Ключ. После того как каждый робот захватит свой первый инструмент, имитируйте небольшую 100-миллисекундную задержку, будто он занят первичной настройкой. Как только робот попытается захватить второй инструмент, он должен громко сообщить об этом в консоль, чтобы вы знали о его намерениях.

Запустите оба потока одновременно. Ваша программа должна продемонстрировать, как при определенном стечении обстоятельств роботы могут оказаться в тупике, ожидая друг друга бесконечно, и выведет последовательность их сообщений о захвате и попытках захвата инструментов.

#### Требования:
1. Программа должна содержать два отдельных объекта-ресурса, которые будут использоваться в качестве "замков" для Гаечного Ключа и Отвертки.
2. В программе должны быть созданы и запущены два отдельных потока: один для Робота Альфа, другой для Робота Бета.
3. Робот Альфа в своём потоке должен сначала захватывать (блокировать) Гаечный Ключ, затем, после паузы, пытаться захватить Отвертку.
4. Робот Бета в своём потоке должен сначала захватывать (блокировать) Отвертку, затем, после паузы, пытаться захватить Гаечный Ключ.
5. После успешного захвата первого инструмента каждый робот должен делать паузу продолжительностью 100 миллисекунд.
6. Перед тем как попытаться захватить второй инструмент, поток должен вывести в консоль сообщение о намерении захватить второй инструмент.
7. Программа должна быть реализована так, чтобы при определённом порядке выполнения потоков возникала ситуация взаимной блокировки (deadlock), когда оба робота навсегда ожидают освобождения второго инструмента друг другом.
8. Программа должна выводить в консоль сообщения о захвате и попытках захвата инструментов обоими роботами, чтобы можно было проследить развитие тупиковой ситуации.