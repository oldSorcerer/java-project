
### Доступ к Секретной Комнате: Логирование входа и выхода из критической секции 🚪

Представьте, что в вашей системе есть "Секретная Комната" (критическая секция), доступ к которой может получить только один "посетитель" (поток) за раз, чтобы избежать хаоса и конфликтов. Вам нужно отслеживать, кто и когда входит и выходит из этой комнаты.

Создайте класс SecretRoomAccess, внутри которого будет метод, имитирующий какую-то работу, требующую эксклюзивного доступа. Этот метод должен быть обёрнут в synchronized-блок, чтобы гарантировать, что только один поток может выполнять его код в любой момент времени. В самом начале synchronized-блока, когда поток "входит" в Секретную Комнату, он должен чётко объявить в консоль: "Поток [имя потока] входит в Секретную Комнату!".

После выполнения всех важных операций и непосредственно перед выходом из synchronized-блока, поток должен так же чётко заявить: "Поток [имя потока] выходит из Секретной Комнаты!". Это поможет вам визуализировать, как потоки по очереди получают доступ к общему ресурсу.

#### Требования:
1. Необходимо создать публичный класс с именем SecretRoomAccess.
2. В классе SecretRoomAccess должен быть метод, имитирующий работу, для которой требуется эксклюзивный доступ (например, void accessRoom()).
3. Внутри метода должен использоваться synchronized-блок, чтобы только один поток мог одновременно выполнять код внутри этого блока.
4. В самом начале synchronized-блока должно выводиться сообщение: "Поток [имя потока] входит в Секретную Комнату!", где [имя потока] — имя текущего потока (Thread.currentThread().getName()).
5. Непосредственно перед выходом из synchronized-блока должно выводиться сообщение: "Поток [имя потока] выходит из Секретной Комнаты!", где [имя потока] — имя текущего потока (Thread.currentThread().getName()).
6. Внутри synchronized-блока должно быть действие, имитирующее работу (например, Thread.sleep или другая операция), чтобы демонстрировать, что поток находится в критической секции.