
### Инвентарь животных: живой список или статичный снимок? 🐾

Представьте, что вы ведете инвентаризацию домашних животных в приюте. У вас есть основной, изменяемый список всех питомцев. Начните с создания обычного ArrayList для вашего petInventory и добавьте туда "кошку" и "собаку".

Теперь вам нужно создать два разных способа просмотра этого инвентаря. Первый — это живое представление, которое всегда будет показывать актуальный список животных, но при этом запретит случайные изменения через него. Используйте Collections.unmodifiableList, чтобы создать livePetView. Второй — это снимок инвентаря на текущий момент, который останется неизменным, независимо от того, какие новые животные поступят в приют. Для этого используйте List.copyOf, создав snapshotPetList.

После того, как вы сформировали оба представления, в ваш основной petInventory добавляется новый житель — "мышь". В завершение, выведите на экран содержимое livePetView и snapshotPetList. Это позволит вам наглядно увидеть разницу в поведении между живым представлением, которое реагирует на изменения в оригинальной коллекции, и фиксированным снимком, который остается таким, каким был в момент его создания.

#### Требования:
1. Необходимо создать изменяемый список petInventory типа ArrayList и добавить в него элементы "кошка" и "собака".
2. Требуется создать переменную livePetView как неизменяемое представление (unmodifiableList) на основе petInventory с помощью Collections.unmodifiableList.
3. Нужно создать переменную snapshotPetList как неизменяемый список (immutable), который фиксирует текущее состояние petInventory на момент создания, используя List.copyOf.
4. После создания обоих представлений в petInventory необходимо добавить новый элемент "мышь".
5. Необходимо вывести на экран содержимое livePetView и snapshotPetList, чтобы продемонстрировать разницу между "живым" представлением и "снимком".
6. Программа не должна позволять изменять содержимое ни через livePetView, ни через snapshotPetList (при попытке изменения должно выбрасываться исключение).