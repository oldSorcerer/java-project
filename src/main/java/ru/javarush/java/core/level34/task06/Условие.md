
### Журнал событий в реальном времени 📈

Вообразите, что вы отслеживаете поток событий в системе реального времени, где новые события могут постоянно поступать, даже когда вы обрабатываете старые. Вам нужна коллекция, которая позволит вам безопасно "читать" текущие события, не беспокоясь о том, что добавление новых событий во время чтения приведет к ошибкам конкурентной модификации.

Создайте экземпляр CopyOnWriteArrayList для хранения вашего "журнала событий". В качестве начальных событий добавьте в него строки "Событие A", "Событие B" и "Событие C". Теперь ваша задача — пройтись по всем текущим событиям в журнале, используя цикл for-each. Но есть подвох: прямо внутри этого цикла, как бы имитируя поступление нового события, добавьте в eventLog ещё одну строку, например, "Новое событие D".

После того как цикл завершится, выведите на экран всё содержимое вашего CopyOnWriteArrayList. Вы увидите, что CopyOnWriteArrayList создает "снимок" коллекции для итерации, позволяя добавлениям происходить без нарушения текущего прохода, а новые элементы будут видны только в последующих итерациях или после завершения текущей. Это демонстрирует, как эта коллекция обеспечивает потокобезопасность при модификации во время чтения.

#### Требования:
1. В программе должен быть импортирован класс java.util.concurrent.CopyOnWriteArrayList.
2. Для хранения событий должен быть создан объект типа CopyOnWriteArrayList<String>.
3. В созданную коллекцию должны быть добавлены строки "Событие A", "Событие B" и "Событие C" до начала итерации.
4. По коллекции CopyOnWriteArrayList необходимо пройтись с помощью цикла for-each.
5. Во время каждой итерации цикла for-each в коллекцию должен добавляться новый элемент, например, строка "Новое событие D".
6. Программа не должна выбрасывать ConcurrentModificationException или других ошибок при добавлении нового элемента во время итерации.
7. После завершения цикла программа должна вывести на экран все элементы коллекции CopyOnWriteArrayList, чтобы показать итоговое состояние журнала событий.
8. В результате выполнения задачи должно быть видно, что новые элементы, добавленные внутри цикла, не участвуют в текущей итерации, а появляются только в коллекции после завершения прохода.