
### Алгоритм шагов: прямое и обратное управление 🔄

Представьте, что вы работаете с последовательностью шагов в сложном алгоритме, и иногда вам нужно просмотреть эти шаги в обратном порядке. Однако, если вы внесете изменения в обратный порядок, эти изменения должны отразиться и на оригинальной последовательности шагов.

Начните с создания SequencedCollection из строк, представляющих ваши начальные шаги алгоритма: "Шаг A", "Шаг B" и "Шаг C". Это будет ваш algorithmSteps. Теперь получите "обратное представление" этой последовательности, используя метод reversed(). Это создаст reversedStepsView. Важно понимать, что это не копия, а именно представление.

Далее, чтобы проверить эту "живую" связь, добавьте новый элемент, например, "Новый Шаг D", именно в reversedStepsView.

Наконец, чтобы наглядно продемонстрировать важную особенность reversed() представления (что оно является "живым" представлением исходной коллекции, а не её независимой копией), выведите на экран содержимое как исходной коллекции algorithmSteps, так и её обратного представления reversedStepsView. Вы увидите, что добавление элемента в reversedStepsView фактически изменило и исходную коллекцию algorithmSteps, просто поместив новый элемент в соответствующую позицию с точки зрения обратного порядка.

#### Требования:
1. Необходимо создать объект типа SequencedCollection<String> и добавить в него элементы "Шаг A", "Шаг B" и "Шаг C" в указанном порядке.
2. Требуется получить обратное представление исходной коллекции с помощью метода reversed() и сохранить его в переменную reversedStepsView.
3. В обратное представление reversedStepsView необходимо добавить новый элемент "Новый Шаг D".
4. После добавления элемента в reversedStepsView, необходимо вывести содержимое как исходной коллекции algorithmSteps, так и её обратного представления reversedStepsView, чтобы показать, что изменения отражаются в обеих коллекциях.
5. Реализация не должна создавать новую независимую коллекцию для reversedStepsView — обратное представление должно быть связано с исходной коллекцией и отражать все изменения.