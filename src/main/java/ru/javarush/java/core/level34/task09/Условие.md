
### Умный диспетчер задач: проверка на пустоту перед работой 🤖

Вы управляете списком задач, которые необходимо выполнить, и прежде чем пытаться получить какую-либо задачу, вы всегда должны убедиться, что список не пуст.

Создайте пустую SequencedCollection типа String. Это будет ваш taskQueue. Теперь ваша задача — проверить, пуста ли эта очередь. Используйте для этого соответствующий метод. Если taskQueue действительно пуст, выведите на экран сообщение: "Коллекция задач пуста". Это позволит вам избежать ошибок при попытке обращения к несуществующим элементам.

В противном случае, если очередь задач не пуста (хотя в данном случае она останется пустой, но логика должна быть универсальной), ваша программа должна была бы вывести первый и последний элементы из этой коллекции. Конечно, для этой конкретной задачи, поскольку коллекция останется пустой, эта часть кода не будет выполнена, но она демонстрирует правильный подход к работе с SequencedCollection перед доступом к её элементам.

#### Требования:
1. Необходимо создать пустую коллекцию типа SequencedCollection<String> с именем taskQueue.
2. Для проверки, содержит ли коллекция элементы, должен быть использован метод isEmpty().
3. Если коллекция taskQueue пуста, программа должна вывести на экран сообщение: "Коллекция задач пуста".
4. Если коллекция taskQueue не пуста, программа должна вывести первый и последний элементы этой коллекции.
5. Для получения первого и последнего элемента коллекции должны быть использованы соответствующие методы SequencedCollection (getFirst(), getLast() или их аналоги).