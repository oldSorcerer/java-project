
### Конфигурация игры: фиксируем настройки релиза 🎮

Вы разрабатываете конфигурацию для новой игры, и у вас есть набор текущих настроек, которые вы можете изменять в процессе разработки. Создайте изменяемую карту (Map), где ключами будут названия настроек (строки), а значениями — их параметры (числа). Например, "одна" со значением 1, "две" со значением 2 и "три" со значением 3.

Представьте, что пришло время «зафиксировать» эти настройки для публичного релиза. Вам нужна абсолютно стабильная версия, которую нельзя будет случайно изменить. Используйте Map.copyOf, чтобы получить неизменяемую копию вашей текущей карты настроек. Это будет ваша «релизная» версия.

Теперь, чтобы убедиться, что однажды «опубликованные» настройки остаются неизменными, попробуйте добавить в вашу «релизную» копию новую пару, например, "четыре" со значением 4. Вы должны ожидать, что это вызовет ошибку. Если попытка добавления завершится исключением, перехватите его и выведите на экран сообщение: "Ошибка: карта неизменяема". Это подтвердит, что ваша релизная конфигурация защищена.

В процессе дальнейшей разработки вы решаете изменить значение одной из настроек в вашей исходной, изменяемой карте, например, обновить значение для ключа "одна" на 100. После этого изменения, выведите на экран содержимое как вашей исходной «рабочей» карты, так и «релизной» неизменяемой копии. Это покажет вам, что изменения в исходной карте никак не повлияли на её ранее созданную неизменяемую копию.

#### Требования:
1. Необходимо создать изменяемую Map<String, Integer>, в которую добавить пары "одна"=1, "две"=2, "три"=3.
2. На основе текущей изменяемой карты настроек нужно создать новую карту с помощью Map.copyOf, чтобы получить неизменяемую версию.
3. Попытка добавить новую пару ("четыре", 4) в неизменяемую карту должна привести к выбросу исключения. Исключение необходимо перехватить и вывести сообщение "Ошибка: карта неизменяема".
4. После создания неизменяемой копии требуется изменить значение по ключу "одна" в исходной карте на 100.
5. После изменения исходной карты необходимо вывести содержимое обеих карт (исходной и неизменяемой), чтобы показать, что изменения в исходной карте не затронули неизменяемую копию.