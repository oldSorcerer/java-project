
### Плейлисты: набор неизменяем, но песни внутри меняются 🎶

Представьте, что вы организуете набор музыкальных плейлистов для своего стримингового сервиса. Вы решили, что сам набор плейлистов (их количество и порядок) должен быть фиксированным и неизменяемым, но содержимое каждого отдельного плейлиста (песни в нем) может свободно меняться.

Ваша задача — создать "внешний" неизменяемый список, который будет содержать лишь один элемент: ваш первый "внутренний" плейлист. Этот "внутренний" плейлист должен быть изменяемым списком строк, например, ArrayList. Сначала создайте пустой ArrayList для вашего myFirstPlaylist.

Затем создайте "внешний" неизменяемый список allPlaylists, используя List.of, и поместите в него ваш myFirstPlaylist. На этом этапе allPlaylists уже нельзя будет изменить, то есть добавить или удалить плейлисты из него. Теперь, после того как allPlaylists стал неизменяемым, добавьте новую песню, например, "hello", непосредственно в ваш "внутренний" плейлист (myFirstPlaylist).

В конце выведите на экран содержимое как внешнего списка allPlaylists, так и внутреннего списка myFirstPlaylist. Это наглядно покажет, что, хотя сам внешний список остался неизменным (вы не можете добавить в него новый плейлист), вы все еще можете модифицировать объекты, которые он содержит, в данном случае — добавлять песни в ваш плейлист. Это важный нюанс при работе с коллекциями и их изменяемостью.

#### Требования:
1. В программе должен быть создан изменяемый список строк myFirstPlaylist с помощью класса ArrayList.
2. В программе должен быть создан внешний список allPlaylists с помощью List.of, содержащий единственный элемент — ссылку на myFirstPlaylist.
3. После создания allPlaylists попытка добавить или удалить элементы из этого списка должна приводить к ошибке (UnsupportedOperationException).
4. После создания allPlaylists в myFirstPlaylist должна быть добавлена строка "hello" с помощью метода add.
5. В конце работы программы на экран должно быть выведено содержимое both allPlaylists и myFirstPlaylist, чтобы продемонстрировать, что внутренний список изменился, а внешний — нет.