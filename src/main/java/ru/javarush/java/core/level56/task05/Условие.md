
### Секретарь-скороход: асинхронный подсчёт строк в огромном отчёте 📊

Представьте, что вы помогаете занятому дата-аналитику, которому срочно нужно узнать, сколько строк содержится в гигантском текстовом отчёте "big_text.txt", занимающем многие гигабайты. Он не может ждать, пока вся программа прочитает файл целиком, и ему нужна информация как можно быстрее, не блокируя его работу.

Ваша программа должна асинхронно, блок за блоком (по 4096 байт), читать этот огромный файл, используя AsynchronousFileChannel. Главная цель — подсчитать общее количество строк, то есть символов новой строки ('\n'). Учтите, что строки могут разрываться между блоками, поэтому для корректной обработки таких ситуаций вам понадобится CharsetDecoder. Как только весь файл будет обработан и подсчёт завершён, программа должна вывести на экран итоговое количество строк в файле, чтобы аналитик мог продолжить свою работу.

#### Требования:
1. Программа должна использовать AsynchronousFileChannel для асинхронного чтения файла.
2. Файл "big_text.txt" должен читаться блоками по 4096 байт.
3. Необходимо корректно учитывать случаи, когда символ новой строки ('\n') может находиться на стыке двух блоков, используя для этого CharsetDecoder.
4. Подсчёт количества строк (символов '\n') должен происходить по мере асинхронного чтения данных, без блокировки основного потока.
5. После завершения обработки всего файла программа должна вывести на экран итоговое количество строк.
6. Для декодирования байтов в символы должен использоваться CharsetDecoder, чтобы избежать ошибок при разрывах многобайтовых символов.
7. Программа должна быть способна корректно работать с очень большими файлами, не загружая их полностью в память.