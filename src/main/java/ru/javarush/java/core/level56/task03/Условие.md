
### Асинхронная проверка начала файла с уведомлением 🕵️‍♀️

Представьте, что ваше приложение обрабатывает большие медиафайлы или объемные архивы, и вам нужно быстро получить предварительный просмотр их содержимого, не блокируя пользовательский интерфейс. Для этого необходимо асинхронно прочитать лишь начальную часть файла и быть уведомленным о результате.

Ваша задача — открыть файл "input.txt" для чтения, используя AsynchronousFileChannel. Затем, с помощью метода read, вы должны асинхронно прочитать первые 128 байт этого файла. Ключевой момент здесь — использовать CompletionHandler, чтобы обработать результат операции. Если чтение прошло успешно, в методе completed вашего обработчика должно появиться сообщение: "Файл успешно прочитан, байт: X", где X — точное количество байт, которое удалось прочитать. В случае возникновения любой ошибки чтения, метод failed должен вывести "Ошибка чтения файла: Y", где Y будет содержать подробное описание произошедшей проблемы.

#### Требования:
1. Файл "input.txt" должен быть открыт для чтения с помощью AsynchronousFileChannel.
2. Необходимо инициировать асинхронное чтение первых 128 байт файла с помощью метода read.
3. Для обработки завершения операции чтения должен использоваться CompletionHandler.
4. В методе completed CompletionHandler должно выводиться сообщение "Файл успешно прочитан, байт: X", где X — количество реально прочитанных байт.
5. В методе failed CompletionHandler должно выводиться сообщение "Ошибка чтения файла: Y", где Y — описание возникшей ошибки.