
### Мониторинг системных журналов: параллельная проверка и экстренное прекращение 🚨

Вы разрабатываете утилиту для диагностики системы, которая должна одновременно проверять два важных системных журнала: "file1.txt" и "file2.txt". Журнал "file1.txt" критически важен, и его чтение должно быть завершено любой ценой, тогда как чтение "file2.txt" менее приоритетно, и его можно отменить, если оно займет слишком много времени.

Ваша задача — запустить две асинхронные операции чтения, каждую через свой AsynchronousFileChannel. Для первой операции ("file1.txt") используйте CompletionHandler, чтобы получить уведомления о завершении. Для второй операции ("file2.txt") также используйте CompletionHandler, но дополнительно получите объект Future<Integer> и через 50 миллисекунд попытайтесь отменить эту операцию.

В каждом CompletionHandler, в методе failed, если произошла ошибка, выведите имя файла и описание проблемы. Если же вторая операция ("file2.txt") была успешно отменена, перехватите исключение CancellationException и выведите "Чтение file2.txt отменено".

После того как обе операции (успешные, ошибочные или отменённые) завершатся, обязательно закройте все открытые каналы, чтобы освободить системные ресурсы.

#### Требования:
1. Для чтения обоих файлов ("file1.txt" и "file2.txt") должны быть созданы отдельные экземпляры AsynchronousFileChannel.
2. Обе операции чтения должны быть инициированы с помощью методов AsynchronousFileChannel и обрабатываться через собственные реализации CompletionHandler.
3. Для "file1.txt" CompletionHandler должен корректно обрабатывать успешное завершение и ошибку (методы completed и failed), при ошибке выводить имя файла и описание проблемы.
4. При запуске асинхронного чтения "file2.txt" необходимо получить объект Future<Integer> и через 50 миллисекунд попытаться отменить операцию с помощью метода cancel.
5. В CompletionHandler для "file2.txt" при отмене операции должно быть перехвачено исключение CancellationException, и выведено сообщение "Чтение file2.txt отменено".
6. В методе failed каждого CompletionHandler при любой ошибке (кроме отмены) должно быть выведено имя соответствующего файла и описание возникшей проблемы.
7. После завершения обеих асинхронных операций (успешно, с ошибкой или отменой) необходимо закрыть оба AsynchronousFileChannel для освобождения ресурсов.