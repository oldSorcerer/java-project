
### Асинхронная запись телеметрических данных с метаданными 🛰️

Вы управляете сложной системой сбора телеметрических данных со спутника. После обработки важного расчёта вам необходимо немедленно записать его результат в файл. При этом очень важно, чтобы в случае успеха или неудачи вы точно знали, к какому файлу относится эта операция, без необходимости искать его имя где-то ещё.

Ваша задача — создать файл "async_result.txt" и асинхронно записать в него строку "CompletionHandler example". Используйте AsynchronousFileChannel и метод write. Но самое интересное: в качестве "вложения" (attachment) к этой асинхронной операции вы должны передать имя целевого файла — строку "async_result.txt". Это позволит вашему CompletionHandler, который будет обрабатывать результат, всегда знать, с каким файлом он работает.

Если запись пройдёт успешно, обработчик должен вывести сообщение "Успешно записано X байт в файл async_result.txt", где X — количество записанных байт. В случае ошибки, выведите "Ошибка записи в файл async_result.txt: Y", где Y — описание проблемы.

#### Требования:
1. В программе должен использоваться класс AsynchronousFileChannel для асинхронной записи данных в файл.
2. Для записи данных должен быть использован метод write класса AsynchronousFileChannel.
3. В качестве attachment к асинхронной операции записи должно быть передано строковое имя файла "async_result.txt".
4. Результат асинхронной операции должен обрабатываться с помощью собственного CompletionHandler.
5. В случае успешной записи CompletionHandler должен вывести сообщение "Успешно записано X байт в файл async_result.txt", где X — количество записанных байт.
6. В случае ошибки CompletionHandler должен вывести сообщение "Ошибка записи в файл async_result.txt: Y", где Y — описание ошибки.
7. Файл "async_result.txt" должен быть создан в процессе выполнения программы (если он не существует).
8. В файл "async_result.txt" должна быть записана строка "CompletionHandler example".