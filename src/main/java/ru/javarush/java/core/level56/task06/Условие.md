
### Автоматизированная фабрика данных: чтение, обработка и асинхронная выгрузка 🤖

Вы разрабатываете автоматизированную систему обработки данных, которая получает сырые данные с датчиков, быстро их анализирует и сохраняет краткий отчёт. Все это должно происходить максимально эффективно, не задерживая поток данных.

Ваша задача — создать программу, которая асинхронно читает файл "data.txt" небольшими блоками по 4096 байт. По мере чтения, она должна подсчитывать количество строк (символов '\n') в этом файле. После того как весь файл будет прочитан и количество строк определено, программа должна асинхронно записать этот результат в новый файл "data_result.txt". Формат записи должен быть "Количество строк: X\n", где X — найденное вами количество строк. Для всех операций чтения и записи используйте AsynchronousFileChannel и убедитесь, что кодировка UTF-8 применяется корректно. Как только результат будет успешно записан, ваша программа должна вывести радостное сообщение: "Результат записан асинхронно!", подтверждая завершение цикла обработки.

#### Требования:
1. Файл "data.txt" должен читаться асинхронно с помощью AsynchronousFileChannel.
2. Чтение данных из файла "data.txt" должно выполняться блоками по 4096 байт каждый.
3. В процессе чтения необходимо подсчитывать количество символов '\n' в файле для определения количества строк.
4. После завершения чтения и подсчета строк результат должен быть записан в файл "data_result.txt" асинхронно с использованием AsynchronousFileChannel.
5. В файл "data_result.txt" необходимо записать строку в формате "Количество строк: X\n", где X — вычисленное количество строк.
6. Все операции чтения и записи файлов должны корректно использовать кодировку UTF-8.
7. После успешной асинхронной записи результата программа должна вывести на экран сообщение: "Результат записан асинхронно!".