
### Объединение фрагментов великой саги 📖

Вы — хранитель древних историй, и перед вами стоит задача собрать воедино разрозненные главы великой саги. Сейчас эта история, "эпическая_сага.txt", хранится в виде нескольких абзацев, каждый из которых записан на отдельной строке.

Ваша Java-программа должна выступить в роли мистического ткача, который способен сплести все эти фрагменты в единое, непрерывное повествование. Она должна прочитать содержимое файла "эпическая_сага.txt" целиком, объединив все абзацы и строки в одну большую строку. Затем программа должна вывести этот цельный, непрерывный текст на экран, представив всю мощь и красоту саги в одном взгляде.

#### Требования:
1. Программа должна открыть текстовый файл с именем "эпическая_сага.txt" для чтения его содержимого.
2. Программа должна прочитать все строки файла полностью, не пропуская ни одной строки.
3. Программа должна объединить все строки файла в одну большую строку, удаляя все символы переноса строки между ними.
4. Программа должна вывести получившуюся объединённую строку (непрерывное повествование) на экран.