
### Поиск общих секретов в шпионских досье 🕵️

Вы — главный криптограф, и вам поручено найти общие секреты в двух досье: "досье_А.txt" и "досье_Б.txt". Оба досье содержат несколько строк с зашифрованными сообщениями.

Напишите Java-программу, которая выступит в роли вашего аналитического центра. Она должна тщательно прочитать содержимое обоих досье, рассматривая каждое досье как набор отдельных сообщений (строк). Ваша цель — найти и вывести на экран только те сообщения, которые являются идентичными и присутствуют в обоих досье. Это позволит выявить ключевые связи и раскрыть заговор!

#### Требования:
1. Программа должна читать оба файла ("досье_А.txt" и "досье_Б.txt") построчно, рассматривая каждую строку как отдельное сообщение.
2. Программа должна использовать имена файлов "досье_А.txt" и "досье_Б.txt" для открытия и чтения данных.
3. Программа должна сравнивать строки из обоих файлов по полному содержимому, считая идентичными только те строки, которые полностью совпадают.
4. Программа должна определить, какие строки встречаются и в первом, и во втором файле.
5. На экран должны быть выведены только те строки, которые присутствуют в обоих файлах и совпадают по содержанию.
6. Если одна и та же строка встречается несколько раз, программа может вывести её только один раз (если требуется уникальность общих сообщений).