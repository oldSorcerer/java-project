
### Каталогизация домашней библиотеки 📚

Вы решили навести порядок в своей обширной домашней библиотеке и создать для этого цифровой помощник.

Для начала вам нужно определить, как будет храниться информация о каждой книге. Создайте специальный класс-запись, который будет содержать название книги и имя её автора.

Затем в основной части вашей программы создайте экземпляр этой записи, например, для вашей любимой книги, указав её название и автора.

В завершение, выведите всю информацию об этой книге на экран, чтобы убедиться, что запись была создана корректно.

#### Требования:
1. Для хранения информации о книге необходимо создать record-класс, а не обычный класс.
2. Record-класс должен содержать два компонента: название книги и имя автора.
3. В основной части программы необходимо создать экземпляр record-класса, передав в конструктор название книги и имя автора.
4. Программа должна вывести информацию о книге (название и автора) на экран.
5. Поля record-класса должны быть неизменяемыми (final по умолчанию, сеттеров быть не должно).