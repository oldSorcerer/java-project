
### Проверка совпадения координат на карте 🗺️

Представьте, что вы работаете над картографическим приложением, где важно точно определять местоположение. Вам нужно удобно представлять координаты и убедиться, что две точки, находящиеся в одном и том же месте, считаются идентичными.

Создайте класс-запись, который будет представлять собой точку на карте с координатами по горизонтали и вертикали.

В основной программе создайте два объекта точек с абсолютно одинаковыми значениями координат. Затем, используя встроенный метод сравнения, проверьте, являются ли эти две точки одинаковыми. Выведите результат сравнения на экран, чтобы подтвердить их идентичность.

#### Требования:
1. Для представления точки на карте необходимо создать record-класс с двумя полями: координатой по горизонтали и координатой по вертикали.
2. В основной программе должны быть созданы два объекта record-класса с одинаковыми значениями координат.
3. Для проверки идентичности точек должен быть использован встроенный метод сравнения (equals), предоставляемый record-классом.
4. Результат сравнения двух точек должен быть выведен на экран.