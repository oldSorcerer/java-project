
### Печать содержимого документа 📄

Вы разрабатываете систему управления документами, где различные типы файлов должны уметь выводить своё содержимое на экран или на печать. Для этого сначала определите общий контракт для всех печатаемых объектов: создайте интерфейс, который будет содержать всего один метод — print().

Затем создайте класс-запись, который будет представлять собой документ, содержащий текстовое содержимое. Сделайте так, чтобы этот класс-запись реализовывал созданный вами интерфейс.

Внутри реализации метода print() настройте вывод так, чтобы на экран сначала выводилась строка "Документ: ", а затем само содержимое документа.

В основной программе создайте экземпляр такого документа с любым текстовым содержанием и вызовите его метод печати, чтобы продемонстрировать его работу.

#### Требования:
1. Необходимо создать интерфейс, содержащий единственный абстрактный метод print() без параметров.
2. Требуется создать класс-запись (record), который содержит поле для текстового содержимого документа.
3. Record-класс документа должен реализовывать ранее объявленный интерфейс с методом print().
4. Внутри record-класса необходимо реализовать метод print(), который выводит на экран строку "Документ: ", а затем текстовое содержимое документа.
5. В основной программе должен быть создан экземпляр record-документа с произвольным текстовым содержимым, после чего должен быть вызван его метод print().