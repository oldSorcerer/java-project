
### Отслеживание заказов в службе доставки дронами 📦

Вы разрабатываете центральную систему управления для парка дронов, которые доставляют заказы. Каждый дрон, выполняя свою миссию, должен всегда "знать" свой уникальный идентификатор заказа (например, "Заказ-1", "Заказ-2", "Заказ-3"), чтобы его действия могли быть правильно логированы и отслежены.

Вам нужно смоделировать запуск нескольких (например, трех) таких дронов, каждый из которых будет выполнять свою задачу параллельно с другими. Для каждого дрона убедитесь, что его уникальный идентификатор заказа доступен только ему самому в процессе выполнения задачи, и что при каждом действии он четко сообщает, какой именно заказ он сейчас обрабатывает, например: "Выполняется задача для Заказа: Заказ-1". Это демонстрирует, как каждый дрон может нести свои собственные уникальные контекстные данные, не путаясь с другими.

#### Требования:
1. В задаче должны быть использованы Scoped Values (java.lang.ScopedValue) для передачи уникального идентификатора заказа каждому потоку дрона.
2. Уникальный идентификатор заказа должен быть доступен исключительно в рамках выполнения задачи конкретного дрона и не должен пересекаться с идентификаторами других дронов.
3. Необходимо реализовать параллельный запуск задач для нескольких дронов (например, трех), чтобы каждый дрон выполнял свою миссию независимо от других.
4. Каждое действие дрона должно сопровождаться выводом сообщения, в котором явно указывается, какой заказ сейчас обрабатывается (например, "Выполняется задача для Заказа: Заказ-1").
5. Контекстные данные (идентификатор заказа) должны передаваться с помощью Scoped Values, а не через ThreadLocal или глобальные переменные.
6. Дроны должны запускаться в отдельных потоках, предпочтительно с использованием виртуальных потоков (Virtual Threads), доступных в Java 21+.