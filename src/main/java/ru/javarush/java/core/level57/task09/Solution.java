package ru.javarush.java.core.level57.task09;

// Демонстрация временного повышения уровня доступа с помощью ScopedValue
public class Solution {
    // Объявляем "контейнер" для значения уровня доступа, привязанного к области выполнения


    public static void main(String[] args) {
        // Устанавливаем базовый уровень доступа "Внешний" для всей демонстрации.
        // Все вызовы внутри run() будут видеть это значение по умолчанию.

            // Перед защищенной операцией
                    // Ожидается: "Внешний"

            // Выполняем защищенную операцию, где уровень временно станет "Внутренний"


            // После завершения защищенной операции значение автоматически восстановлено
                    // Ожидается: "Внешний"
        
    }

    // Защищенная операция: временно повышаем уровень доступа в рамках этой функции
    static void protectedOperation() {
        // Временное повышение уровня доступа — действует только внутри этого run()

            // Внутри защищенной операции

         // По выходу из run() значение ACCESS_LEVEL автоматически возвращается к прежнему
    }
}