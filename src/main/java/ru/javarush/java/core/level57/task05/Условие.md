
### Мониторинг населения виртуального города 🏙️

Представьте, что вы разрабатываете симулятор огромного виртуального города, где каждый житель – это легкий виртуальный поток, который на мгновение "оживает" для выполнения какого-то действия (например, покупает кофе или идет на работу).

Вам нужно смоделировать активность 1000 таких жителей, каждый из которых "засыпает" на одну секунду, имитируя свою занятость.

После того как все "жители" начнут свою деятельность, ваша задача – выяснить, сколько именно этих виртуальных сущностей в данный момент находятся в активном состоянии внутри вашей виртуальной машины, используя специальные инструменты Java для подсчета всех виртуальных потоков.

Выведите это число на экран, чтобы увидеть масштаб вашего виртуального города.

#### Требования:
1. Для моделирования жителей города должны использоваться виртуальные потоки (Virtual Threads), а не обычные потоки.
2. В программе должно быть создано ровно 1000 виртуальных потоков, каждый из которых выполняет свою задачу.
3. Каждый виртуальный поток должен "засыпать" (sleep) на одну секунду, имитируя занятость жителя.
4. Все виртуальные потоки должны быть запущены примерно одновременно, чтобы их активность пересекалась.
5. После запуска всех потоков программа должна определить количество виртуальных потоков, находящихся в активном состоянии на данный момент, используя соответствующие средства Java (например, Thread API, ThreadMXBean и т.д.).
6. Программа должна вывести на экран число активных виртуальных потоков внутри виртуальной машины.