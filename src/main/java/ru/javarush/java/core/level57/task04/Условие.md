
### Управление экспериментальным роботом-помощником 🤖

Вы создаете экспериментального робота-помощника, и иногда его внутренние процессы могут давать сбой.

Запустите один из его фоновых модулей в виде виртуального потока, который, к сожалению, обязательно столкнется с критической ошибкой и "выбросит" исключение RuntimeException с сообщением "Ошибка в потоке: модуль неисправен!".

Несмотря на это происшествие, ваш главный управляющий поток должен спокойно дождаться, пока неисправный модуль завершит свою работу (или крашнется), а затем, несмотря ни на что, вывести на экран жизнеутверждающее сообщение: "Основной поток завершён. Робот перезапускается.".

Это покажет, как ваша система умеет справляться с неожиданными сбоями в фоновых задачах.

#### Требования:
1. В программе должен быть создан виртуальный поток с помощью Thread.ofVirtual().start().
2. В виртуальном потоке должна быть запущена задача (Runnable), имитирующая работу модуля робота.
3. В задаче виртуального потока должно быть выброшено исключение RuntimeException с сообщением "Ошибка в потоке: модуль неисправен!".
4. Главный поток должен дождаться завершения виртуального потока (в том числе и при возникновении исключения) с помощью метода join().
5. После завершения работы виртуального потока главный поток должен вывести на экран сообщение: "Основной поток завершён. Робот перезапускается."