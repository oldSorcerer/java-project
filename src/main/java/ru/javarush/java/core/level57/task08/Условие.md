
### Мониторинг сборочной линии роботов-манипуляторов 🦾

Представьте, что вы главный инженер на автоматизированном заводе, где 100 роботов-манипуляторов выполняют различные сборочные операции. Каждая операция уникальна и имеет свой порядковый номер от 1 до 100.

Однако, существует одна загвоздка: некоторые операции (каждая пятнадцатая по счету, то есть 15-я, 30-я, 45-я и так далее) являются критически сложными и могут приводить к сбоям в работе манипулятора. Если операция N кратна 15, манипулятор сообщает об ошибке "Ошибка в задаче N". В остальных случаях, он успешно завершает свою работу и отчитывается: "Задача N выполнена".

Ваша задача – запустить все 100 операций одновременно, используя эффективное управление потоками. После того как вы инициировали все задачи, ваш центральный мониторинговый пульт должен дождаться завершения каждой из них.

Затем, для каждой операции, система должна проверить ее статус: если манипулятор столкнулся с ошибкой, выведите на экран точное сообщение о сбое, например: "Задача N провалена: Ошибка в задаче N". Если же операция завершилась успешно, просто подтвердите это сообщением: "Задача N успешно завершена".

После полного аудита всех операций, необходимо аккуратно выключить систему управления роботами.

#### Требования:
1. Необходимо использовать ExecutorService, создаваемый с помощью Executors.newVirtualThreadPerTaskExecutor(), для запуска всех задач.
2. Программа должна инициировать выполнение 100 задач (операций), каждая из которых соответствует уникальному номеру от 1 до 100.
3. Каждая задача с номером, кратным 15, должна выбрасывать исключение с сообщением "Ошибка в задаче N".
4. Задачи с номерами, не кратными 15, должны завершаться успешно и возвращать/выводить сообщение "Задача N выполнена".
5. Центральная система должна дождаться завершения выполнения всех задач (использовать методы ожидания завершения, например, invokeAll или сбор Future-объектов с последующим get).
6. По завершении всех задач необходимо для каждой операции проверить результат: если задача завершилась с ошибкой, вывести "Задача N провалена: Ошибка в задаче N", если успешно — "Задача N успешно завершена".
7. После обработки всех результатов программа должна корректно завершить работу ExecutorService (вызвать shutdown()).
8. При получении результата выполнения задач необходимо корректно обрабатывать исключения, чтобы не прерывать аудит остальных задач.