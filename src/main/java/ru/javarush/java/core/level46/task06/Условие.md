
### Создание публичных профилей в социальной сети 🌐

Вы работаете над созданием модуля для публичных профилей в новой социальной сети. В этих профилях должна отображаться только некоторая информация о человеке, а другие, внутренние данные, должны оставаться скрытыми от посторонних глаз.

Создайте Java-класс SocialNetworkUser с полями для имени (userFirstName, тип String), возраста (userAge, тип int) и статуса занятости (isEmployed, тип boolean).

Однако для публичных профилей нужно, чтобы отображались только имя и возраст пользователя. Используйте аннотацию @Expose и пометьте ею только поля userFirstName и userAge. Поле isEmployed оставьте без этой аннотации, так как оно не должно быть видно в публичном JSON.

Далее, создайте коллекцию, например, List, и добавьте в нее два экземпляра SocialNetworkUser с разными данными, чтобы показать разнообразие профилей.

Теперь самое интересное: для сериализации этого списка в JSON вам понадобится использовать GsonBuilder. Настройте его так, чтобы он исключал из JSON все поля, которые не помечены аннотацией @Expose, и чтобы полученный JSON был красиво отформатирован, с отступами, для удобства чтения. Выведите итоговую JSON-строку на экран.

Убедитесь, что в результате отображаются только имя и возраст пользователя, а информация о занятости отсутствует, и весь JSON выглядит аккуратно.

#### Требования:
1. Необходимо создать класс SocialNetworkUser с тремя полями: userFirstName (String), userAge (int), isEmployed (boolean).
2. Поля userFirstName и userAge должны быть помечены аннотацией @Expose.
3. Поле isEmployed не должно быть помечено аннотацией @Expose, чтобы оно не попадало в публичный JSON.
4. Необходимо создать коллекцию (например, List) и добавить в нее минимум два объекта SocialNetworkUser с разными значениями полей.
5. GsonBuilder должен быть настроен так, чтобы сериализовать только поля, помеченные @Expose (метод excludeFieldsWithoutExposeAnnotation).
6. GsonBuilder должен быть настроен на вывод красиво отформатированного (pretty printing) JSON.
7. Коллекция SocialNetworkUser должна быть сериализована в JSON, и итоговая строка JSON должна быть выведена на экран.
8. В итоговом JSON-выводе должны присутствовать только поля userFirstName и userAge, а поле isEmployed должно отсутствовать.