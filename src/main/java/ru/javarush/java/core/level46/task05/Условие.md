
### Синхронизация данных о студентах с внешней системой 🎓

Представьте, что вы — администратор образовательной платформы, и вам нужно передавать данные о студентах в стороннюю систему. Эта система, однако, ожидает немного специфичные имена полей для входящих данных.

Ваша задача — создать Java-класс SchoolStudent с полями для имени студента (studentName, тип String) и его среднего балла (studentGrade, тип int).

Но есть нюанс: когда вы будете отправлять данные студента в JSON, система-получатель ждет, что поле с именем студента будет называться "student_name", а не просто "studentName". Чтобы решить эту задачу элегантно, используйте аннотацию @SerializedName("student_name") для поля studentName.

После этого создайте объект SchoolStudent, заполните его произвольными, но осмысленными значениями, например, "Мария Иванова" и 92 балла. Затем используйте библиотеку Gson для сериализации этого объекта в JSON-строку и выведите её на экран. Убедитесь, что в итоговом JSON имя поля, обозначающее студента, действительно имеет вид "student_name", как того требует сторонняя система.

#### Требования:
1. Необходимо создать класс с именем SchoolStudent.
2. Класс SchoolStudent должен содержать два поля: studentName типа String и studentGrade типа int.
3. Поле studentName должно быть аннотировано с помощью @SerializedName("student_name") для задания специфичного имени поля при сериализации.
4. Требуется создать объект класса SchoolStudent и присвоить ему значения: имя "Мария Иванова" и средний балл 92.
5. Необходимо использовать библиотеку Gson для сериализации объекта SchoolStudent в JSON-строку.
6. В результирующей JSON-строке имя студента должно быть представлено как "student_name", а не "studentName".
7. Сериализованная JSON-строка должна быть выведена на экран.