
### Обработка списка городов для путеводителя 🗺️

Вы разрабатываете умное приложение-путеводитель, которое получает информацию о различных городах мира в виде JSON-массива. Вот пример данных, которые вам приходят из внешнего источника: [{"city":"Moscow"},{"city":"Berlin"}]. Ваша задача — извлечь названия всех этих городов и вывести их на экран, чтобы пользователь мог их просмотреть и выбрать следующее направление для путешествия.

Используя гибкую библиотеку Jackson и её сердце, ObjectMapper, десериализуйте этот JSON-массив в коллекцию List<Map<String, Object>>. После того как данные будут успешно загружены, пройдитесь по каждому элементу этого списка и для каждого элемента извлеките значение, хранящееся по ключу "city". Каждое найденное название города выведите на экран с новой строки, чтобы ваш путеводитель мог четко показать пользователю все доступные направления.

#### Требования:
1. В программе должен быть импортирован класс ObjectMapper из библиотеки Jackson.
2. JSON-массив вида [{"city":"Moscow"},{"city":"Berlin"}] должен быть десериализован в объект типа List<Map<String, Object>> с помощью ObjectMapper.
3. Для каждого элемента списка (каждой Map) необходимо получить значение по ключу "city".
4. Каждое извлечённое название города должно быть выведено на экран с новой строки, в том порядке, в котором они идут в исходном JSON.
5. В качестве структуры данных для хранения результатов десериализации должен использоваться List<Map<String, Object>>.