
### Магический приемник импульсов 🔮

После того как вы успешно определили структуру MagicalPulseEvent, следующим шагом в вашей магической системе связи является создание механизма для приёма и обработки этих импульсов. Представьте, что различные магические устройства или даже сами маги должны уметь "слушать" эфир на предмет появления новых импульсов и реагировать на них.

Вам предстоит создать MagicalPulseListener — это будет магический интерфейс, который расширяет java.util.EventListener. Внутри этого интерфейса объявите один-единственный, но крайне важный метод: void onMagicalPulse(MagicalPulseEvent pulse). Этот метод станет стандартным способом для любого объекта реагировать на входящий магический импульс.

Далее, оживите простой класс, назовите его SimpleMageListener. Этот класс должен реализовать ваш MagicalPulseListener интерфейс. Внутри метода onMagicalPulse, когда он будет вызван, SimpleMageListener должен симулировать "чтение" полученного импульса, выводя в консоль сообщение вроде "Магический импульс получен: " с последующим текстом самого сообщения из импульса. Это покажет, что ваш магический приёмник успешно обрабатывает входящие данные.

#### Требования:
1. Необходимо создать интерфейс с именем MagicalPulseListener.
2. Интерфейс MagicalPulseListener должен расширять интерфейс java.util.EventListener.
3. В интерфейсе MagicalPulseListener должен быть объявлен единственный метод void onMagicalPulse(MagicalPulseEvent pulse).
4. Класс SimpleMageListener должен реализовывать интерфейс MagicalPulseListener.
5. В классе SimpleMageListener метод onMagicalPulse должен быть переопределён.
6. Метод onMagicalPulse в классе SimpleMageListener должен выводить в консоль строку "Магический импульс получен: " с добавлением текста сообщения из объекта MagicalPulseEvent.