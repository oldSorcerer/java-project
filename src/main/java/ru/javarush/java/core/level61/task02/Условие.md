
### Цифровой Следопыт 📁

Вы — отважный цифровой следопыт, и перед вами стоит задача исследовать содержимое таинственной директории. Но есть загвоздка: вы не знаете, на какой операционной системе ваш инструмент будет работать!

Ваша программа должна быть достаточно умной, чтобы выбрать правильную команду. Если вы оказались на территории Windows, используйте команду cmd.exe /c dir. Если же вы на любой другой системе (например, Linux или macOS), ваш верный помощник — команда ls -l.

Создайте программу, которая запустит нужную команду через ProcessBuilder и затем покажет вам все, что скрывается внутри директории, выведя результат выполнения команды прямо на экран. Это позволит вам увидеть, какие файлы и папки лежат перед вами, независимо от типа "цифровой местности".

#### Требования:
1. Программа должна определять, на какой операционной системе она запущена (Windows или другая).
2. Если программа запущена на Windows, она должна использовать команду "cmd.exe /c dir" для отображения содержимого директории.
3. Если программа запущена не на Windows (например, Linux или macOS), она должна использовать команду "ls -l" для отображения содержимого директории.
4. Программа должна запускать выбранную команду с помощью класса ProcessBuilder.
5. Программа должна вывести на экран результат работы внешней команды (список файлов и папок в директории).