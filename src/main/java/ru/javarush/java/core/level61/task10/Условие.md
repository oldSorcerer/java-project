
### Всевидящее Око Детектива 🕵️

Вы — цифровой детектив, расследующий деятельность подозрительного внешнего процесса. Этот процесс (например, команда java -version, которая выводит информацию и в обычный вывод, и в ошибки) может выдавать улики сразу в два канала: стандартный вывод (stdout) и поток ошибок (stderr).

Ваша задача — быть "всевидящим оком" и одновременно читать информацию из обоих потоков, чтобы ни одна деталь не ускользнула от вашего внимания. Организуйте параллельное чтение, используя отдельные потоки (Thread), для каждого канала. После того как процесс завершит свою работу, аккуратно закройте все используемые ресурсы и убедитесь, что все ваши "ниточки" (потоки чтения) также завершили свою работу. Каждая строка, прочитанная из stdout, должна быть помечена [stdout], а из stderr — [stderr]. Это позволит вам получить полный и структурированный отчет о деятельности подозреваемого процесса.

#### Требования:
1. Необходимо создать и запустить внешний процесс (например, с помощью ProcessBuilder или Runtime.exec).
2. Вывод внешнего процесса из стандартного потока вывода (stdout) и потока ошибок (stderr) должен читаться одновременно с помощью отдельных потоков (Thread).
3. Каждая строка, считанная из stdout, должна выводиться с префиксом [stdout], а из stderr — с префиксом [stderr].
4. После завершения процесса необходимо дождаться окончания работы потоков чтения (использовать join).
5. Все используемые потоки ввода-вывода (InputStream, BufferedReader и т.д.) должны быть корректно закрыты после завершения чтения.
6. Необходимо дождаться завершения внешнего процесса (использовать Process.waitFor или аналогичные методы).