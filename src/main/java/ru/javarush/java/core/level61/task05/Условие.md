
### Аварийная Остановка Системы 🚨

Представьте, что вы — оператор критически важной системы. Некий фоновый процесс (например, команда ping google.com с параметрами -c 100 для Linux/macOS или без -c для Windows, чтобы она работала достаточно долго) начал внезапно потреблять слишком много ресурсов или завис.

Ваша задача — запустить этот процесс, но быть готовым к немедленной реакции. Через две секунды после старта (используя Thread.sleep(2000)), вы должны принудительно остановить этот "непослушный" процесс, используя метод destroy(). Затем, чтобы убедиться, что он действительно прекратил свое существование, дождитесь его окончательного завершения с помощью waitFor().

Как только убедитесь, что угроза миновала, выведите на экран триумфальное сообщение: "Процесс был завершён принудительно".

#### Требования:
1. Программа должна запускать длительный процесс (например, ping с параметрами, чтобы он работал не менее 2 секунд), подходящий для текущей операционной системы.
2. Для создания и запуска внешнего процесса необходимо использовать класс ProcessBuilder и его методы.
3. После запуска процесса программа должна приостановить выполнение на 2 секунды с помощью Thread.sleep(2000).
4. Через 2 секунды после запуска программа обязана вызвать метод destroy() у объекта Process для завершения процесса.
5. После вызова destroy() программа должна дождаться полного завершения процесса с помощью метода waitFor().
6. После того как процесс завершён, программа должна вывести на экран сообщение: "Процесс был завершён принудительно".