
### Тайное Послание 📩

Представьте, что вы — тайный агент, которому нужно отправить короткое, но очень важное сообщение в засекреченный терминал. Этот терминал (внешний процесс, например, cat или его аналог в вашей ОС) ждет вашего ввода, чтобы затем повторить его.

Ваша миссия: запустить этот внешний процесс и, не прикасаясь к клавиатуре, передать ему через стандартный вход (stdin) строго определенную строку: "Input from Java". После этого вам нужно завершить передачу, словно вы нажали Enter, и затем внимательно прочитать стандартный вывод (stdout) терминала.

Ваша программа должна доказать, что сообщение дошло и было обработано, выведя на экран именно ту строку, которую вы отправили: "Input from Java".

#### Требования:
1. Программа должна запускать внешний процесс (например, cat или его аналог), который читает данные из стандартного ввода и выводит их в стандартный вывод.
2. Программа должна передать процессу через его стандартный вход (stdin) строку "Input from Java" без участия пользователя.
3. После передачи строки "Input from Java" программа должна корректно завершить ввод (например, закрыть поток вывода в процесс).
4. Программа должна считать стандартный вывод (stdout) внешнего процесса.
5. Программа должна вывести на экран строку "Input from Java", полученную из stdout внешнего процесса.