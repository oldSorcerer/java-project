
### Диспетчер Космической Станции 🚀

Вы — главный диспетчер космической станции, и в вашем распоряжении экспериментальный модуль. Этот модуль (внешний процесс) способен передавать данные как о штатной работе (обычные сообщения), так и о критических неполадках (ошибки). Вам крайне важно отслеживать оба потока информации одновременно, чтобы ничего не упустить.

Запустите внешний процесс (например, команду, которая пишет и в stdout, и в stderr, или выполните echo Hello && wrongcommand через оболочку).

Ваша задача — создать программу, которая будет параллельно (используя отдельные потоки) читать стандартный вывод (stdout) и стандартный поток ошибок (stderr) этого процесса. Каждое полученное сообщение должно быть помечено: [stdout] для обычных данных и [stderr] для сообщений об ошибках, чтобы вы могли моментально понять источник информации.

Это позволит вам получить полную и своевременную картину состояния модуля, убедившись, что все сообщения из обоих каналов отображаются на вашем пульте управления.

#### Требования:
1. Программа должна запускать внешний процесс, который выводит данные как в стандартный поток вывода (stdout), так и в поток ошибок (stderr).
2. Программа должна одновременно читать как стандартный поток вывода (stdout), так и поток ошибок (stderr) внешнего процесса, используя для этого отдельные потоки (например, два потока Thread или аналоги).
3. Каждое сообщение, считанное из stdout, должно выводиться на экран с префиксом [stdout], а каждое сообщение из stderr — с префиксом [stderr].
4. Программа должна отображать все сообщения из обоих потоков, не теряя ни одного сообщения ни из stdout, ни из stderr.
5. Программа должна корректно завершать работу после окончания внешнего процесса, убедившись, что все сообщения из обоих потоков были прочитаны и выведены.