
### Голоса внутреннего мира 🎭

Представьте, что вы — драматург, исследующий, как разные голоса в вашей программе представляют себя. Есть основной рассказчик (ваш главный класс Main), который имеет свое собственное имя. Но иногда на сцене появляется таинственный анонимный персонаж, который также имеет свое имя и говорит от своего имени. А потом сам рассказчик может высказать быструю мысль. Вам нужно понять, как каждый из них говорит "Я" и чье имя при этом называет.

Создайте класс Main и объявите в нём строковую переменную name, присвоив ей значение "Main". Внутри специального метода test() этого класса, сделайте следующее:

Представьте, что вы создаете того самого таинственного анонимного персонажа, используя анонимный класс, реализующий Runnable. У этого персонажа также будет своя переменная name, но со значением "Anonymous". Когда этот персонаж "заговорит" (то есть выполнится его метод run()), он должен выдать на экран this.name.
Сразу после этого, дайте возможность самому рассказчику высказать быструю мысль, используя лямбда-выражение типа Runnable. Когда эта "мысль" будет "высказана" (выполнен run()), она также должна вывести на экран this.name.
Последовательно вызовите run() для обоих созданных объектов и внимательно наблюдайте за тем, чье имя будет произнесено в каждом случае. Ожидаемый результат: сначала "Anonymous", затем "Main".

#### Требования:
1. В классе Main должна быть объявлена строковая переменная name со значением "Main".
2. В классе Main должен быть объявлен метод test(), в котором реализуется вся логика задачи.
3. В методе test() должен быть создан объект типа Runnable с использованием анонимного класса, внутри которого объявлена своя строковая переменная name со значением "Anonymous".
4. При вызове run() у объекта анонимного класса на экран должно быть выведено значение переменной name, объявленной внутри анонимного класса ("Anonymous").
5. В методе test() должен быть создан второй объект типа Runnable с использованием лямбда-выражения.
6. При вызове run() у объекта-лямбды на экран должно быть выведено значение переменной name, объявленной в классе Main ("Main").
7. В методе test() сначала должен быть вызван run() у анонимного класса (вывод "Anonymous"), затем run() у лямбда-выражения (вывод "Main").