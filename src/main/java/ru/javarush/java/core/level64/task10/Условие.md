
### Ошибка кэширования с HashMap – Музей, Который Никогда Не Выбрасывает Экспонаты 🖼️

Вы строите "музей памяти" для вашего приложения, используя обычный HashMap для кэширования данных. Ваша идея в том, чтобы хранить там часто используемые объекты. Но есть одна особенность: этот музей настолько бережливый, что никогда ничего не выбрасывает! Даже если экспонат потеряет всю свою популярность, и на него не будет ссылаться ни один посетитель, музей будет упрямо держать его у себя, занимая драгоценное место.

Ваша задача: создать класс CacheDemo с полем cache типа HashMap<Object, Object>. В главном методе вашей программы main добавьте в этот кэш 1000 новых объектов в цикле. Эти объекты не должны иметь никаких других ссылок за пределами вашего кэша – они существуют только внутри него.

Затем попробуйте "очистить" память, вызвав System.gc(), как будто вы просите уборщика убрать всё ненужное. Программа должна вывести текущий размер кэша сразу после добавления всех 1000 элементов, а затем снова вывести размер кэша после вызова System.gc(). Вы должны убедиться, что размер кэша не уменьшится, ведь HashMap держит "крепкие" ссылки на все свои "экспонаты", не позволяя сборщику мусора их убрать, даже если на них больше нет других ссылок.

#### Требования:
1. Необходимо создать публичный класс с именем CacheDemo.
2. В классе CacheDemo должно быть объявлено поле cache типа HashMap<Object, Object>.
3. В методе main необходимо добавить 1000 новых объектов в HashMap cache, причем эти объекты не должны иметь других ссылок, кроме как в кэше.
4. Сразу после добавления всех элементов программа должна вывести размер кэша (количество элементов в HashMap).
5. В методе main должно быть вызвано System.gc() после вывода размера кэша.
6. После вызова System.gc() программа должна снова вывести размер кэша.
7. Размер кэша после вызова System.gc() не должен уменьшиться, что подтверждает: HashMap хранит сильные ссылки на объекты, и сборщик мусора их не удаляет.