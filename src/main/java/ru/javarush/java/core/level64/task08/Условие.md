
### Просмотр статистики по классам через jmap – Инвентаризация Цифрового Зоопарка 🐒🐘🐆

Вы – управляющий большим цифровым зоопарком (Java-приложением) и хотите узнать, какие "виды животных" (классы объектов) занимают больше всего места в вашей "памяти-вольере". Возможно, у вас есть слишком много каких-то "обезьян" (строк) или "слонов" (крупных объектов), и вы хотите это выяснить, чтобы оптимизировать пространство.

Ваша задача: запустить Java-приложение, которое намеренно создаст очень много объектов одного типа (например, огромный массив строк или 1000 экземпляров какого-то простого класса). Пусть это приложение работает, не завершаясь.

Затем, используя команду jmap -histo <PID>, где <PID> – это идентификатор вашего запущенного приложения, получите "инвентаризационную ведомость". Эта команда выведет таблицу, показывающую, сколько объектов каждого класса существует в памяти и какой объем они занимают.

Ваша цель – просмотреть этот список и найти "зверя", который является самым большим "пожирателем памяти". В комментарии к вашему коду укажите пример этой команды и напишите, какой класс, по вашим наблюдениям, занял больше всего места.

#### Требования:
1. Необходимо написать Java-приложение, которое намеренно создает большое количество объектов одного типа (например, массив строк или множество экземпляров пользовательского класса).
2. Приложение должно работать в бесконечном цикле или иным образом не завершаться самостоятельно, чтобы была возможность анализировать его память во время выполнения.
3. Требуется использовать команду jmap -histo <PID> для получения статистики по количеству и размеру объектов в памяти приложения, где <PID> — идентификатор запущенного процесса Java-приложения.
4. Необходимо просмотреть вывод команды jmap -histo и определить, какой класс занимает наибольший объем памяти в куче.
5. В комментарии к коду требуется привести пример использованной команды jmap и указать, какой класс оказался самым "памятистым" по результатам анализа.