
### StackOverflowError – Озорной Каскад Рекурсии 🧙‍♂️

Представьте, что вы пишете магическое заклинание, которое должно что-то делать, а потом, не подумав, снова вызывать само себя, без какого-либо условия для остановки. Это похоже на эхо, которое бесконечно усиливается, или мяч, который прыгает все выше и выше, никогда не останавливаясь.

Ваша задача — создать Java-программу, где один метод (например, endlessEcho или recursiveCurse) вызывает сам себя рекурсивно, но абсолютно без остановки.

Запустите эту программу и посмотрите, что произойдет. Когда ваше "заклинание" выйдет из-под контроля, Java-машина выбросит особое исключение. Ваша задача – определить, что это за исключение, и вписать его название в виде // комментария прямо в ваш код.

#### Требования:
1. В программе должен быть объявлен метод (например, endlessEcho или recursiveCurse), который вызывает сам себя рекурсивно.
2. Рекурсивный метод не должен содержать никаких условий для завершения рекурсии (например, if, return при определённых условиях и т.п.).
3. В методе main программы должен быть вызван рекурсивный метод для запуска бесконечной рекурсии.
4. В результате выполнения программы должно возникать исключение StackOverflowError.
5. Название возникшего исключения (StackOverflowError) должно быть вписано в код в виде комментария.