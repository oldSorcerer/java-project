
### Снятие дампа памяти с помощью jmap – Цифровой Слепок Медведя 🐻

Представьте, что вы – опытный зоолог, изучающий повадки редкого зверя – запущенного Java-приложения, которое внезапно начало себя странно вести, например, чрезмерно потреблять память. Чтобы понять, что происходит внутри этого "зверя", вам нужно сделать его точный цифровой слепок – дамп памяти, который покажет все его внутренности в определенный момент времени.

Ваша задача: запустить любое простое Java-приложение (можно даже просто System.out.println("Running...") в бесконечном цикле, чтобы оно не завершалось). Узнайте его идентификатор процесса (PID). Затем, используя инструмент командной строки jmap, выполните команду для создания полного дампа памяти этого приложения в бинарном формате. Вам понадобится команда jmap -dump:format=b,file=heap.bin <PID>. В результате в вашей текущей директории должен появиться файл heap.bin, который и будет вашим "цифровым слепком" – его можно будет анализировать в будущем. В комментарии к вашему файлу укажите пример этой команды, чтобы другие "зоологи" знали, как её использовать.

```
jmap -dump:format=b,file=heap.bin <PID>
```

#### Требования:
1. Необходимо создать и запустить простое Java-приложение, которое не завершает свою работу сразу (например, с бесконечным циклом).
2. Требуется определить PID запущенного Java-приложения, например, с помощью команды jps или task manager.
3. Необходимо воспользоваться утилитой jmap и выполнить команду вида jmap -dump:format=b,file=heap.bin <PID>, где <PID> — идентификатор процесса Java.
4. В результате выполнения команды jmap должен появиться файл heap.bin в текущей директории.
5. В файле (или в отчёте) должен быть приведён пример используемой команды jmap для снятия дампа памяти.