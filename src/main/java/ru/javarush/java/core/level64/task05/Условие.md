
### Использование слабой ссылки – Послание на Исчезающих Чернилах 👻

Представьте, что вы хотите написать секретное послание, которое будет существовать, пока на него активно кто-то смотрит или хранит в памяти. Но как только все отвернутся и забудут о нем, оно должно волшебным образом исчезнуть, освободив место. В мире Java для этого существуют "слабые ссылки".

Ваша задача — создать в программе обычную строку с любым текстом, например, "Weak reference example", которая будет вашим секретным посланием. Затем создайте "слабую ссылку" (WeakReference) на эту строку. После этого вы должны "забыть" о своем основном послании, установив все сильные ссылки на него в null. Затем попросите сборщик мусора (System.gc()) выполнить свою работу, как будто он пришел проверить, что можно убрать. Наконец, попробуйте "прочитать" ваше послание через слабую ссылку. Выведите результат на экран. Если сборщик мусора успешно справился со своей задачей, и никто больше не держал сильную ссылку на объект, то ваше послание должно было исчезнуть (т.е. weakReference.get() вернет null).

#### Требования:
1. В программе должна быть создана строка с любым текстом, например, "Weak reference example".
2. На созданную строку должна быть создана слабая ссылка с помощью класса WeakReference.
3. Все сильные ссылки на строку должны быть установлены в null после создания слабой ссылки.
4. В программе должен быть явно вызван сборщик мусора с помощью System.gc() после обнуления сильных ссылок.
5. После вызова сборщика мусора необходимо попытаться получить объект через weakReference.get() и вывести результат на экран.
6. Программа должна корректно обрабатывать ситуацию, когда weakReference.get() возвращает null (объект удалён сборщиком мусора).