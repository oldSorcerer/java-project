
### Узнаём используемый сборщик мусора – Детектив JVM 🕵️

Вы работаете детективом в мире Java, и вам нужно выяснить, какая именно "уборочная бригада" (сборщик мусора, или Garbage Collector) по умолчанию следит за чистотой памяти в вашей виртуальной машине. Это похоже на то, как вы пытаетесь узнать, кто отвечает за уборку в большом офисном здании, просто взглянув на их отчеты.

Ваша миссия: запустить простую Java-программу с особым флагом, который позволит JVM "поделиться" этой информацией. После запуска внимательно изучите логи консоли. В комментарии к вашему Java-коду укажите полную команду запуска программы, включая нужный флаг, а также приведите пример строки из лога, по которой можно однозначно определить тип используемого сборщика мусора. Пусть ваша программа будет максимально простой, например, просто печатать "Hello, GC Detective!".

```
// Пример простейшей программы
public class GcDetective {
    public static void main(String[] args) {
        System.out.println("Hello, GC Detective!");
    }
}
```

#### Требования:
1. В комментарии к коду должна быть указана команда запуска Java-программы с флагом, позволяющим определить используемый сборщик мусора (например, -XX:+PrintGCDetails или аналогичный).
2. Программа должна быть максимально простой и должна выводить на экран строку "Hello, GC Detective!".
3. В комментарии к коду должен быть приведён пример строки из лога JVM, по которой можно однозначно определить тип используемого сборщика мусора.
4. В коде обязательно должен быть комментарий, содержащий как команду запуска с нужным флагом, так и пример строки из лога.
5. В программе не должно быть дополнительной логики, кроме вывода приветственной строки и комментария с инструкцией.