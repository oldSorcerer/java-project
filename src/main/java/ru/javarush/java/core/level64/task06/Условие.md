
### Кэш с использованием WeakHashMap – Умная Библиотека, Которая Перерабатывает Забытые Книги 📚

Вы разрабатываете интеллектуальную систему кэширования для вашего приложения – эдакую умную библиотеку, которая хранит часто используемые данные, но при этом умеет автоматически освобождать место, если эти данные больше никому не нужны. Это намного эффективнее, чем вручную чистить кэш, и это ключ к созданию отзывчивых приложений.

Ваша задача: реализовать простой класс SimpleCache. Внутри этого класса должно быть поле cache, которое будет особой "картой" — WeakHashMap<String, String>. Добавьте в этот класс два метода: put(String key, String value) для добавления новой информации и get(String key) для её извлечения.

В главном методе вашей программы создайте экземпляр SimpleCache. Добавьте в него тестовую пару, например, ("key1", "value1"). Сразу же выведите на экран результат попытки получить данные по "key1", чтобы убедиться, что они в кэше. Затем намеренно "забудьте" про строку "key1", если у вас была сильная ссылка на неё (например, присвоив переменной null), и попросите сборщик мусора System.gc() убрать всё ненужное. После этого снова попробуйте получить данные по "key1" из кэша и выведите результат. Вы должны увидеть, что "умная библиотека" могла выбросить эту "книгу", потому что на неё больше не было сильных ссылок извне, и get("key1") теперь может вернуть null.

#### Требования:
1. Поле cache класса SimpleCache должно быть объектом типа WeakHashMap<String, String>.
2. Класс SimpleCache должен содержать публичные методы put(String key, String value) для добавления пары в кэш и get(String key) для получения значения по ключу.
3. В главном методе программы необходимо создать экземпляр SimpleCache, добавить в него пару ("key1", "value1") и вывести результат вызова get("key1") на экран.
4. После добавления пары "key1"-"value1" необходимо удалить сильную ссылку на строку "key1" (например, присвоить переменной, содержащей "key1", значение null).
5. В программе должен быть вызван System.gc() после удаления сильной ссылки на ключ.
6. После вызова сборщика мусора необходимо снова попытаться получить значение по ключу "key1" из кэша и вывести результат на экран. Если объект был удалён, результат должен быть null.