
### OutOfMemoryError – Ненасытный Пожиратель Памяти 💾

Вы работаете над сверхсовременной системой анализа данных, которая должна собирать информацию. Но есть одна загвоздка: вы забыли указать, когда ей нужно остановиться! Представьте, что это робот-коллекционер, который пытается сохранить каждый пиксель вселенной, или библиотекарь, который бесконечно заказывает новые книги, не имея места для хранения.

Создайте Java-программу, в которой вы в бесконечном цикле будете добавлять в список ArrayList огромные массивы целых чисел (например, int[] размером в 1_000_000 элементов каждый). Ваша задача — наблюдать, как программа постепенно "задыхается" от переполнения памяти. Через некоторое время, когда память компьютера будет полностью исчерпана, Java-машина выбросит исключение. Укажите имя этого исключения в виде комментария в вашем коде, чтобы задокументировать, что произойдет, когда ваш "пожиратель памяти" достигнет предела.

#### Требования:
1. Программа должна содержать бесконечный цикл, в котором выполняются действия по добавлению данных в память.
2. В программе должен использоваться объект типа ArrayList для хранения ссылок на массивы.
3. В каждом проходе цикла в ArrayList должен добавляться новый массив типа int[] размером не менее 1 000 000 элементов.
4. Добавление новых массивов должно приводить к постепенному увеличению потребления оперативной памяти программой.
5. Программа должна быть реализована таким образом, чтобы в результате исчерпания памяти JVM выбрасывалось исключение OutOfMemoryError.
6. В коде должен присутствовать комментарий с указанием имени исключения OutOfMemoryError, которое возникнет при переполнении памяти.