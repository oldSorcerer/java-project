
### Опасная очистка базы данных с возможными "пустыми" записями 😼

Представьте, что вы работаете над базой данных имён питомцев для новой мобильной игры. У вас есть список этих имён, но, к сожалению, из-за ошибки в импорте некоторые записи могли быть потеряны или повреждены, оставив после себя null. Ваша задача — обработать все записи, преобразовав их в верхний регистр, чтобы они выглядели единообразно.

Создайте список List<String> и добавьте в него несколько элементов, включая одно или несколько значений null: List.of("cat", null, "dog").

Теперь попробуйте преобразовать все элементы этого списка в верхний регистр с помощью map(String::toUpperCase) и выведите результат на экран. Посмотрите, что произойдет, если вы попытаетесь это сделать "в лоб" с элементами, которые могут отсутствовать. Будьте готовы к тому, что программа выбросит NullPointerException из-за попытки вызвать метод на null. Это важный урок о работе с неопределенными значениями!

#### Требования:
1. В программе должен быть создан список типа List<String>, в который добавлены как строки, так и хотя бы одно значение null.
2. Для преобразования элементов списка в верхний регистр необходимо использовать Stream API и метод map(String::toUpperCase).
3. В задаче требуется попытаться преобразовать все элементы, включая null, в верхний регистр, что приведет к возникновению исключения.
4. В результате выполнения кода с map(String::toUpperCase) на списке, содержащем null, программа должна выбросить исключение NullPointerException.
5. Результат выполнения преобразования (или выброшенное исключение) должен быть выведен на экран.