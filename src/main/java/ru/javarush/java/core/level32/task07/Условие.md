
### Создание бирок для товаров с ограниченными кодами 🏷️

Вы работаете на складе, и вам предстоит организовать новую партию товаров, которые имеют как цвет, так и числовой код. Однако вы обнаруживаете, что количество доступных числовых кодов меньше, чем количество цветов. Вам нужно создать бирки, объединяющие цвет и код, но только для тех товаров, для которых есть и цвет, и соответствующий код, учитывая минимальное количество.

У вас есть два массива:

```
String[] availableColors = {"red", "green", "blue", "yellow"};
int[] itemCodes = {1, 2, 3};
```

Ваша задача — объединить их так, чтобы каждая строка цвета сочеталась с соответствующим числовым кодом, но только для тех пар, для которых хватает обоих элементов. То есть, результат должен быть вида "red-1", "green-2", "blue-3", игнорируя лишние цвета, для которых нет кодов. Используйте IntStream.range для итерации и соберите конечный результат в список finalLabels. Затем выведите этот список на экран.

#### Требования:
1. Для организации итерации по массивам должен использоваться IntStream.range с границей, равной минимальному размеру двух массивов.
2. Для каждой пары элементов с одинаковым индексом из availableColors и itemCodes необходимо создать строку формата "цвет-код" (например, "red-1").
3. Если один из массивов длиннее другого, лишние элементы должны быть проигнорированы — объединение происходит только по минимальному размеру массивов.
4. Результирующие строки должны быть собраны в список с именем finalLabels.
5. Содержимое списка finalLabels должно быть выведено на экран.