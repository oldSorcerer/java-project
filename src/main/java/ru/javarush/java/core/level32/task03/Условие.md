
### Сбор урожая для грандиозного фруктового салата 🍎🍊

Представьте, что вы готовите грандиозный фруктовый салат, и у вас есть две корзины с разными фруктами. Чтобы начать их обработку, вам нужно объединить содержимое обеих корзин в один непрерывный поток, чтобы ни один фрукт не был забыт.

Сначала создайте две переменные, каждая из которых будет представлять собой список строк с названиями фруктов. Пусть в первой корзине будут firstBasketFruits = List.of("Яблоко", "Груша") , а во второй — secondBasketFruits = List.of("Банан", "Апельсин") .

Ваша задача — используя Stream.concat, объединить эти два списка в один большой поток allSaladIngredients. Затем пройдитесь по этому объединенному потоку и выведите название каждого фрукта на отдельной строке.

#### Требования:
1. Необходимо создать две переменные типа List<String> с именами firstBasketFruits и secondBasketFruits, содержащие указанные фрукты.
2. Для объединения двух списков в один поток должен быть использован метод Stream.concat.
3. Необходимо получить поток (stream) из каждой коллекции firstBasketFruits и secondBasketFruits.
4. Результатом объединения двух потоков должен быть новый поток с именем allSaladIngredients.
5. Каждый фрукт из объединенного потока allSaladIngredients должен быть выведен на экран на отдельной строке.