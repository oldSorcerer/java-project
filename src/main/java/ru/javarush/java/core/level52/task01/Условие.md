
### Отладка производственной линии 🏭

Представьте, что вы отвечаете за централизованную систему учёта произведённых виджетов на огромной автоматизированной фабрике. Несколько производственных линий одновременно отправляют данные о завершении работы, и крайне важно, чтобы общий счёт был абсолютно точным.

Чтобы реализовать это, создайте класс WidgetCounter. В нём должен быть публичный целочисленный показатель totalWidgetsProduced, который будет хранить общее количество произведённых изделий. Затем добавьте метод recordProduction(), который безопасно увеличивает totalWidgetsProduced на единицу. Этот метод обязательно должен быть объявлен как synchronized, чтобы предотвратить любые ошибки при одновременном доступе нескольких линий.

В вашей главной программе, имитирующей работу фабрики, создайте один экземпляр WidgetCounter. Затем запустите три отдельные "производственные линии" (то есть, три потока). Каждая из этих линий должна вызвать метод recordProduction() 5000 раз. После того, как все производственные линии завершат свою колоссальную работу, выведите на экран окончательное значение totalWidgetsProduced. Ваша система должна гарантировать, что результат будет ровно 15000, подтверждая идеальный учёт каждого виджета.

#### Требования:
1. В классе WidgetCounter должно быть публичное целочисленное поле totalWidgetsProduced, хранящее общее количество произведённых изделий.
2. В классе WidgetCounter должен быть метод recordProduction(), объявленный с модификатором synchronized.
3. Метод recordProduction() должен увеличивать значение totalWidgetsProduced на единицу при каждом вызове.
4. В главной программе должен быть создан только один экземпляр класса WidgetCounter, который используется всеми потоками.
5. В главной программе необходимо создать и запустить три отдельных потока, имитирующих производственные линии.
6. Каждый из трёх потоков должен вызвать метод recordProduction() ровно 5000 раз.
7. Главная программа должна дождаться завершения всех трёх потоков перед выводом результата.
8. После завершения всех потоков программа должна вывести на экран итоговое значение totalWidgetsProduced, которое должно быть равно 15000.