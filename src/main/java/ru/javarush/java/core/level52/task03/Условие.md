
### Учёт общедоступных пожертвований 🌍

Вы являетесь частью команды, которая разрабатывает глобальную платформу для сбора пожертвований на благотворительность. Все средства, поступающие от разных источников, должны быть аккумулированы в единый, общедоступный фонд.

Для этого создайте класс CharityFund. В нём объявите приватное статическое целочисленное поле totalCollectedAmount, которое будет хранить общую сумму пожертвований. Реализуйте статический синхронизированный метод recordDonation(int amount), который безопасно увеличивает totalCollectedAmount на переданную сумму. Кроме того, создайте статический метод getOverallTotal(), который позволит узнать текущее значение totalCollectedAmount.

В вашей основной программе, имитирующей транзакции, вызовите CharityFund.recordDonation(10), затем CharityFund.recordDonation(5). После этих операций получите и выведите на экран CharityFund.getOverallTotal(). Система должна безупречно отобразить 15, подтверждая, что все пожертвования учтены в центральном фонде.

#### Требования:
1. В классе CharityFund должно быть объявлено приватное статическое целочисленное поле totalCollectedAmount для хранения общей суммы пожертвований.
2. Класс CharityFund должен содержать статический синхронизированный метод recordDonation(int amount), который увеличивает значение totalCollectedAmount на переданную сумму.
3. В классе CharityFund должен быть реализован статический метод getOverallTotal(), который возвращает текущее значение поля totalCollectedAmount.
4. Метод recordDonation(int amount) должен быть синхронизирован для обеспечения корректной работы при одновременном обращении из нескольких потоков.
5. В основной программе должны быть вызваны CharityFund.recordDonation(10) и CharityFund.recordDonation(5), после чего вызов CharityFund.getOverallTotal() должен вернуть 15, и это значение должно быть выведено на экран.