
### Централизованный офисный принтер 🖨️

Представьте, что вы управляете единственным, очень важным общим OfficePrinter в оживлённом корпоративном офисе. Несколько пользователей могут одновременно пытаться отправить задания на печать, но принтер может обрабатывать только одно сообщение за раз, чтобы избежать хаотичного, перемешанного вывода.

Создайте класс OfficePrinter. Внутри него объявите приватное поле Object с именем printerAccessLock, которое будет служить выделенным объектом-монитором для синхронизации. Реализуйте метод printDocument(String documentContent). Этот метод должен принимать documentContent и аккуратно выводить его на экран. Критическая часть: эта операция вывода должна быть заключена в синхронизированный блок synchronized (printerAccessLock). Это гарантирует, что только одно задание на печать может выполняться в любой момент времени, используя именно этот конкретный объект printerAccessLock как привратника.

В вашей основной программе создайте один объект OfficePrinter. Затем сымитируйте попытки печати нескольких пользователей, вызвав printDocument() несколько раз из разных мест вашего основного метода (или даже из отдельных потоков для более реалистичного теста). Вы должны отчётливо видеть, как каждое содержимое документа печатается полностью, одно за другим, без какого-либо наложения, — всё благодаря общему printerAccessLock.

#### Требования:
1. Необходимо создать класс с именем OfficePrinter.
2. В классе OfficePrinter должно быть приватное поле типа Object с именем printerAccessLock, которое используется только для синхронизации.
3. Метод printDocument(String documentContent) должен выводить переданный текст на экран, причём операция вывода должна быть заключена в блок synchronized (printerAccessLock).
4. Благодаря использованию printerAccessLock только один поток может выполнять печать (вывод содержимого) в любой момент времени.
5. В основной программе необходимо создать один объект OfficePrinter и вызвать printDocument() несколько раз из разных потоков, чтобы продемонстрировать корректную синхронизацию вывода.
6. Каждый документ должен выводиться полностью, без перемешивания с выводом других документов.