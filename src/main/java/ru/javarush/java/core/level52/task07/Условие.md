
### Мониторинг критических событий 🚨

Вы разрабатываете надёжную систему для отслеживания критических событий, которые происходят в вашей сети. Множество датчиков могут сообщать о событиях одновременно, и подсчёт eventCount должен быть абсолютно точным, даже при сильной нагрузке.

Спроектируйте класс EventMonitor. Он должен содержать приватное целочисленное поле eventCount и, для управления параллельным доступом, поле типа ReentrantLock. Реализуйте метод recordEvent(), который безопасно увеличивает eventCount на единицу. Этот метод критически важен: он обязательно должен захватывать блокировку, используя lock.lock(), и, что не менее важно, гарантировать вызов lock.unlock() внутри блока try-finally. Это обеспечит, что блокировка будет освобождена, даже если во время инкрементации произойдёт какая-либо ошибка.

В вашей основной программе, имитирующей поток событий, создайте объект EventMonitor. Сымитируйте поток активности, вызвав recordEvent() несколько раз, например, 10. В конце получите и выведите на экран окончательное значение eventCount, чтобы подтвердить, что каждое событие было точно зарегистрировано, демонстрируя надёжность вашего синхронизированного счётчика.

#### Требования:
1. Класс EventMonitor должен содержать приватное целочисленное поле eventCount для хранения количества событий.
2. Класс EventMonitor должен содержать приватное поле типа ReentrantLock для управления параллельным доступом.
3. Метод recordEvent() класса EventMonitor должен увеличивать значение eventCount на единицу.
4. Метод recordEvent() должен захватывать блокировку с помощью lock.lock() и обязательно освобождать её с помощью lock.unlock() внутри блока try-finally.
5. Блокировка ReentrantLock должна быть освобождена даже в случае возникновения исключения во время инкрементации eventCount.
6. В основной программе должен быть создан объект EventMonitor.
7. В основной программе метод recordEvent() должен быть вызван несколько раз (например, десять) для имитации поступления событий.
8. В конце работы программы должно быть получено и выведено на экран итоговое значение eventCount для подтверждения корректности подсчёта событий.