
### Очки в командной игре 🎮

Вы разрабатываете ядро системы подсчёта очков для захватывающей многопользовательской командной игры. Множество игроков одновременно зарабатывают очки для своей команды, и вам необходимо обеспечить, чтобы общее количество очков команды было подсчитано безупречно, без потери даже одного балла.

Для этого создайте класс TeamScoreTracker, который будет содержать публичное целочисленное поле currentTeamScore. В этом классе реализуйте метод addPoint(), который увеличивает currentTeamScore на 1. Важно, чтобы внутри этого метода вы использовали синхронизированный блок synchronized (this), чтобы гарантировать безопасное обновление общего счёта.

В главной программе, моделирующей игровой процесс, создайте один объект TeamScoreTracker. Затем запустите четыре "игровых сессии" (то есть, четыре потока), каждая из которых вызовет метод addPoint() 2500 раз, имитируя интенсивное зарабатывание очков. Как только все игровые сессии завершат свой вклад, выведите на экран итоговое значение currentTeamScore. Ваша программа должна точно отобразить 10000 очков, подтверждая, что каждый балл был учтён.

#### Требования:
1. Необходимо создать публичный класс с именем TeamScoreTracker.
2. В классе TeamScoreTracker должно быть публичное целочисленное поле с именем currentTeamScore.
3. В классе TeamScoreTracker должен быть реализован публичный метод addPoint(), который увеличивает currentTeamScore на 1. Внутри этого метода должен использоваться синхронизированный блок synchronized (this) для обеспечения потокобезопасности.
4. В главной программе должен быть создан один общий объект TeamScoreTracker, который используется всеми потоками.
5. В главной программе должны быть созданы и запущены четыре потока (игровых сессии), каждый из которых вызывает метод addPoint() 2500 раз.
6. Главная программа должна корректно дождаться завершения всех четырёх потоков до вывода результата (например, с помощью метода join()).
7. После завершения всех потоков итоговое значение currentTeamScore должно быть равно 10000.
8. Программа должна вывести итоговое значение currentTeamScore на экран.