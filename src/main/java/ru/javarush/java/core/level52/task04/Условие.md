
### Управление фоновой службой безопасности 🛡️

Представьте, что вы разрабатываете жизненно важную фоновую службу для высокотехнологичной системы безопасности — некий SecurityScanner. Этот сканер должен непрерывно работать в отдельном потоке, отслеживая потенциальные угрозы. Однако вам нужен надёжный способ его остановки по требованию.

Создайте класс SecurityScanner, который должен расширять Thread. Внутри этого класса объявите приватное volatile boolean поле scannerActive, изначально установленное в true. В методе run() реализуйте бесконечный цикл while(scannerActive). Для демонстрации, внутри цикла пока ничего не делайте. Теперь самое главное: создайте публичный метод initiateShutdown(), который будет переводить scannerActive в false, сигнализируя сканеру о необходимости остановки.

В вашей основной программе сначала создайте объект SecurityScanner и запустите его с помощью метода start(). Затем, спустя короткое время, вызовите initiateShutdown(), чтобы дать команду сканеру на завершение работы. Критически важно дождаться полного завершения работы потока сканера, используя метод join(). Как только SecurityScanner корректно остановится, выведите на экран сообщение "Security Scanner has ceased operations.", подтверждая его контролируемое выключение.

#### Требования:
1. Класс SecurityScanner должен быть объявлен как наследник класса Thread.
2. В классе SecurityScanner должно быть приватное поле scannerActive типа volatile boolean, инициализированное значением true.
3. В методе run класса SecurityScanner должен быть реализован цикл while(scannerActive), внутри которого не выполняются никакие действия.
4. В классе SecurityScanner должен быть публичный метод initiateShutdown, который устанавливает значение поля scannerActive в false.
5. В основной программе необходимо создать экземпляр SecurityScanner, запустить его с помощью метода start(), затем вызвать initiateShutdown() после короткой задержки, и дождаться завершения работы потока с помощью метода join().
6. После завершения работы потока SecurityScanner в основной программе должно быть выведено сообщение "Security Scanner has ceased operations."