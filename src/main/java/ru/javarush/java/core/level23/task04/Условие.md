
### Обязательное именование нового товара 🏷️

Вы отвечаете за инвентаризацию в большом онлайн-магазине, и вам нужно строго следить за тем, чтобы каждый новый товар получал свое уникальное имя сразу при добавлении в систему.

Создайте публичный класс Product, который будет представлять каждый товар. Внутри него объявите приватное строковое поле productName — это означает, что имя товара будет доступно только изнутри самого класса Product, чтобы никто не мог изменить его напрямую извне.

Теперь самое важное: реализуйте специальный "строитель" для этого класса, то есть конструктор, который обязательно принимает в качестве параметра строку, представляющую имя товара, и сразу же присваивает её вашему приватному полю productName.

Если вы попытаетесь создать новый товар без указания имени, например, просто new Product();, компилятор не позволит вам этого сделать, он выдаст ошибку, потому что вы не предоставили требуемый параметр.

Только тогда, когда вы создадите объект Product, передав ему конкретное имя товара, например, new Product("Молоко");, ваш товар будет успешно "учтён", и вы сможете вывести его имя на экран, подтверждая успешное создание.

#### Требования:
1. Должен быть создан публичный класс с именем Product.
2. Внутри класса Product должно быть объявлено приватное поле типа String с именем productName.
3. Класс Product должен иметь конструктор, который принимает один параметр типа String — имя товара.
4. Внутри конструктора переданное имя товара должно быть присвоено приватному полю productName.
5. В классе Product не должно быть конструктора без параметров, чтобы нельзя было создать товар без имени.
6. Поле productName должно быть недоступно напрямую извне класса (т.е. только private).
7. Объект класса Product должен создаваться только с указанием имени товара, например, new Product("Молоко"). Попытка создать объект без параметров должна приводить к ошибке компиляции.