
### Кто может "говорить" в зоопарке? 🐾

Вы создаете интерактивную симуляцию зоопарка, где каждый зверь может издавать звуки, но вы хотите, чтобы эта способность была доступна только для самих животных или их "потомков", но не для абсолютно случайных сторонних объектов.

Создайте базовый класс Animal и внутри него объявите защищённый (protected) метод makeSound(), который просто выводит на экран "Some sound". Это означает, что метод доступен из самого класса, его наследников и классов в том же пакете.

Теперь, создайте класс Dog, который наследуется от Animal. Изнутри класса Dog (например, в его собственном методе или конструкторе) попробуйте вызвать метод makeSound(), унаследованный от Animal. Это должно сработать без проблем, потому что Dog является "потомком" Animal.

Затем, создайте совершенно новый, несвязанный класс, который не наследуется от Animal. Попробуйте в этом третьем классе создать объект Animal и вызвать у него makeSound().

Вы заметите, что компилятор выдаст ошибку. Это наглядно демонстрирует, что protected методы доступны для наследников, но остаются "закрытыми" для классов, которые не состоят в родстве наследования или не находятся в том же пакете, тем самым контролируя, кто может "заставить" животных издавать звуки.

#### Требования:
1. Должен быть создан класс Animal.
2. В классе Animal должен быть объявлен метод makeSound() с модификатором доступа protected.
3. Метод makeSound() должен выводить на экран строку "Some sound".
4. Должен быть создан класс Dog, который наследуется от Animal.
5. В классе Dog должен осуществляться вызов метода makeSound(), унаследованного от Animal (например, в методе или конструкторе Dog).
6. Должен быть создан третий класс, который не наследуется от Animal, в котором создается объект Animal и делается попытка вызвать у него метод makeSound().
7. Попытка вызвать метод makeSound() из класса, не являющегося наследником Animal, должна приводить к ошибке компиляции.