
### Метод внутри метода: Архитектурный запрет 🏗️

Вы амбициозный разработчик и решили создать собственный калькулятор. Для начала вы определяете главный класс для его логики, назовите его Calculator, сделав его публичным, чтобы он был доступен отовсюду. Внутри этого Calculator вы планируете создать основной метод, который будет выполнять все расчеты – public void calculate().

Теперь, представьте, что вы пытаетесь быть слишком "креативным" и решаете объявить еще один метод, например, public void printResult(), прямо внутри уже существующего метода calculate(). Ваша IDE или компилятор тут же даст вам знать, что так нельзя! Программа не скомпилируется, потому что в Java методы могут существовать только внутри классов, а не внутри других методов, это фундаментальное правило структуры кода.

#### Требования:
1. В коде должен быть объявлен класс с именем Calculator, помеченный модификатором public.
2. Все методы должны объявляться только на уровне класса Calculator, а не внутри других методов.
3. Внутри метода calculate() не допускается объявление другого метода, например, printResult().
4. В классе Calculator должен присутствовать публичный метод void calculate().
5. Если попытаться объявить метод внутри метода, код не должен компилироваться согласно правилам Java.