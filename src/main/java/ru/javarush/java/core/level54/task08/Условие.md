
### Разгадка криптографического кода 🕵️‍♀️

Вы — мастер-криптограф, и вы перехватили огромный массив числовых кодов — целых 1000 значений, каждое из которых находится в диапазоне от 0 до 100. Ваша миссия — быстро подсчитать, сколько из этих кодов являются четными, так как они тайно обозначают особый тип сообщения.

Поскольку список так велик, вам нужен высокоэффективный параллельный метод для сканирования. Создайте массив, чтобы имитировать ваш перехваченный массив сообщений, заполнив его случайными значениями.

Затем вам нужно реализовать специализированный RecursiveTask<Integer>, который будет представлять ваш передовой аналитический инструмент. Он должен разбивать сложную задачу подсчета четных чисел на более мелкие фрагменты, гарантируя, что ни одна отдельная вычислительная единица не будет перегружена (каждая часть должна обрабатывать не более 100 элементов).

Наконец, разверните всю вашу аналитическую мощь, используя центральный Центр Обработки Задач, чтобы запустить этот процесс и раскрыть, сколько "секретных" четных кодов скрыто в массиве.

#### Требования:
1. Необходимо создать массив из 1000 целых чисел, каждое значение должно быть в диапазоне от 0 до 100 включительно, и заполнить его случайными числами.
2. Для выполнения параллельных вычислений необходимо использовать ForkJoinPool в качестве "Центра Обработки Задач".
3. Необходимо реализовать собственный подкласс RecursiveTask<Integer>, который будет заниматься подсчетом количества четных чисел в заданном диапазоне массива.
4. RecursiveTask должен делить задачу подсчета на более мелкие фрагменты, если размер обрабатываемого диапазона превышает 100 элементов. Для каждого такого фрагмента должен запускаться отдельный подзадача.
5. Каждая вычислительная единица (либо подзадача, либо листовая задача) должна подсчитать количество четных чисел в своем диапазоне массива.
6. Необходимо запустить RecursiveTask через ForkJoinPool и получить итоговый результат — общее количество четных чисел в массиве.
7. Ни одна отдельная вычислительная единица (листовая задача) не должна обрабатывать более 100 элементов массива за одну итерацию.