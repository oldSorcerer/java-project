
### Отправка срочного пакета на дроне 📦

Представьте, что вы диспетчер службы экспресс-доставки, управляющей одним специализированным дроном. Этот дрон предназначен для выполнения только одной задачи за раз, а затем возвращается на базу.

Вам нужно отправить его на срочное задание. Создайте для вашего дрона выделенный пункт управления, способный обрабатывать только одну миссию одновременно.

Запустите миссию, которая просто подтверждает её выполнение сообщением "Задача выполнена!".

Как только дрон закончит и отправит отчет, аккуратно завершите работу его системы управления, чтобы он мог вернуться на зарядку.

#### Требования:
1. Необходимо создать экземпляр ExecutorService, который может выполнять только одну задачу одновременно (например, используя Executors.newSingleThreadExecutor()).
2. Задача для дрона должна быть реализована как объект Callable, возвращающий строку с сообщением "Задача выполнена!".
3. Миссия должна быть отправлена на выполнение через созданный ExecutorService с помощью метода submit.
4. Результат выполнения задачи необходимо получить с помощью объекта Future и вывести полученное сообщение на экран.
5. После завершения работы дрона (получения результата) необходимо корректно завершить работу ExecutorService с помощью метода shutdown.