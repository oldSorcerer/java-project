
### Экспресс-анализ финансового портфеля 💰

Вы — ведущий аналитик в отделе финансового прогнозирования, и вам поступила задача быстро оценить эффективность инвестиционного портфеля. У вас есть список целых чисел, представляющих ключевые числовые показатели, например, ежедневные колебания прибыли.

Ваша задача — используя инновационный Collectors.teeing из Stream API, одновременно вычислить общую сумму всех этих показателей и их среднее значение.

Чтобы аккуратно представить эти два важнейших результата, создайте специальный класс Result с двумя полями: totalSum (типа int) и averageValue (типа double).

После того как все расчёты будут выполнены, выведите на экран оба значения из вашего объекта Result, чтобы ваш отчёт был полным и информативным.

#### Требования:
1. Для вычисления суммы и среднего значений должен быть использован Stream API и коллектор Collectors.teeing.
2. В программе должен быть определён отдельный класс Result с двумя полями: totalSum (int) и averageValue (double).
3. Поле totalSum должно хранить сумму всех элементов списка, а поле averageValue — среднее арифметическое этих элементов.
4. Результаты вычислений (сумма и среднее) должны быть собраны в объект Result с помощью Collectors.teeing.
5. После выполнения расчётов значения totalSum и averageValue из объекта Result должны быть выведены на экран.