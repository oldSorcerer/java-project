
### Сортировка абитуриентов в Академии Великих Достижений 📜

Вы — глава приёмной комиссии в "Академии Великих Достижений", и перед вами стоит важная задача: отсеять кандидатов, основываясь на их академической успеваемости.

Создайте класс Student с методами getStudentName() и getStudentGpa(). Затем у вас есть список потенциальных студентов (добавьте несколько объектов Student с разными именами и GPA).

Ваша цель — используя Stream API и мощные коллекторы Collectors.partitioningBy в сочетании с Collectors.mapping, разделить всех абитуриентов на две группы. В первую группу, под ключом true, попадут имена тех счастливчиков, чей getStudentGpa() составляет не ниже 3.0 — они успешно прошли отбор. Во вторую группу, под ключом false, отправятся имена тех, кто пока не соответствует этим строгим требованиям.

Итогом вашей работы должна стать карта Map<Boolean, List<String>>. После того как вы проведёте эту тщательную сортировку, выведите полученный результат на экран, чтобы ясно видеть, кто принят, а кто нет.

#### Требования:
1. Необходимо создать класс Student с приватными полями для имени и GPA, а также с публичными методами getStudentName() и getStudentGpa().
2. В программе должен быть создан и заполнен список объектов Student с разными именами и значениями GPA.
3. Для обработки списка студентов требуется использовать Stream API.
4. Необходимо применить коллектор Collectors.partitioningBy, чтобы разделить студентов на две группы по критерию GPA >= 3.0.
5. Внутри partitioningBy должен использоваться Collectors.mapping для преобразования объектов Student в их имена (String).
6. Результатом работы должен быть объект Map<Boolean, List<String>>, где ключ true соответствует списку имён студентов с GPA не ниже 3.0, а ключ false — остальным.
7. Полученная карта Map<Boolean, List<String>> должна быть выведена на экран.