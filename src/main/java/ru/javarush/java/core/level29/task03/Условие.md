
### Создание цифрового отпечатка для книг в каталоге 📚

Представьте, что вы усердный библиотекарь, которому поручено организовать обширный цифровой каталог книг. Для эффективного управления и поиска книг вам нужен быстрый способ генерации уникального «цифрового отпечатка» для каждой из них, основанного на её основной идентичности.

Ваш первый шаг — разработать класс Book. Этот класс должен чётко определять два существенных атрибута: bookTitle // строковое значение и bookAuthor // также строковое значение. Суть этой задачи заключается в создании этого уникального отпечатка. Вам нужно переопределить метод hashCode внутри вашего класса Book. Вместо того чтобы самостоятельно создавать сложную логику хеширования, вы будете использовать мощную утилиту Objects.hash, передавая ей как bookTitle, так и bookAuthor для генерации комбинированного хеш-кода. Это гарантирует, что книги с одинаковым названием и автором будут последовательно производить один и тот же отпечаток.

Как только ваш класс Book будет идеально создан, создайте новый объект Book. Присвойте ему конкретное название и автора. Наконец, отобразите вычисленный хеш-код для этого объекта Book на вашем экране. Это продемонстрирует, как ваш цифровой каталог присваивает последовательный идентификатор каждому уникальному литературному произведению.

#### Требования:
1. Необходимо создать публичный класс с именем Book.
2. Класс Book должен содержать два приватных строковых поля: bookTitle и bookAuthor.
3. В классе Book должен быть переопределён метод hashCode.
4. В реализации метода hashCode требуется использовать метод Objects.hash, передавая ему значения полей bookTitle и bookAuthor.
5. В основной программе необходимо создать объект класса Book, задав конкретные значения для названия и автора.
6. После создания объекта Book программа должна вывести на экран результат вызова метода hashCode для этого объекта.