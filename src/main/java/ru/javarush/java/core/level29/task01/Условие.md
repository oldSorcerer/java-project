
### Учёт уникальных городов в виртуальном мире 🗺️

Представьте, что вы главный урбанист в грандиозном виртуальном мире, и ваша задача — вести реестр уникальных городов. Крайне важно, чтобы в вашем реестре один и тот же город не был записан дважды, даже если он представлен разными записями в вашей системе.

Для начала разработайте класс City. Этот класс должен хранить два ключевых атрибута: cityName (строковое значение) и cityPopulation (целое число). Волшебство происходит, когда вы определяете, что делает два города по-настоящему «одинаковыми». Для вашего реестра два объекта City считаются идентичными, если у них совпадают и cityName, и cityPopulation. Чтобы ваша система правильно распознавала дубликаты и управляла ими, вы должны тщательно переопределить методы equals и hashCode внутри вашего класса City.

Теперь давайте проверим вашу систему. Создайте HashSet, специально предназначенный для хранения ваших объектов City. Добавьте в него два совершенно разных объекта, представляющих один и тот же город, то есть дайте обоим объектам одинаковые значения cityName и cityPopulation. Например, вы можете добавить два города "Метрополис", каждый с населением 1000000. После добавления ваша критическая задача — определить, сколько уникальных городов ваш HashSet считает, что он содержит. Отобразите это количество на экране. Если ваши методы equals и hashCode идеально соответствуют вашей задумке, реестр должен гордо сообщить, что в нём хранится только один уникальный город.

#### Требования:
1. В программе должен быть создан класс City с двумя приватными полями: cityName (String) и cityPopulation (int).
2. В классе City должен быть переопределён метод equals так, чтобы два объекта считались равными только если совпадают оба поля: cityName и cityPopulation.
3. В классе City должен быть переопределён метод hashCode так, чтобы он был согласован с логикой equals: объекты с одинаковыми значениями cityName и cityPopulation должны иметь одинаковый hashCode.
4. В программе должен использоваться HashSet<City> для хранения объектов класса City.
5. В HashSet должны быть добавлены два разных объекта City с одинаковыми значениями cityName и cityPopulation.
6. После добавления двух равных по содержимому объектов HashSet должен содержать ровно один элемент.
7. Программа должна вывести на экран количество уникальных городов в HashSet.