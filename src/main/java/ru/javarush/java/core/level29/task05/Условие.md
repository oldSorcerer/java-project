
### Сортировка книжного каталога по нескольким критериям 📖

Будучи прилежным библиотекарем, вы создаёте продвинутый цифровой каталог для своей обширной коллекции. Вам нужен сложный способ сортировки книг: в первую очередь по году их издания, а затем, для книг, выпущенных в один и тот же год, по их названию в алфавитном порядке.

Ваш первый шаг — определить класс Book. Каждый объект Book должен инкапсулировать своё bookTitle (строковое значение) и publicationYear (целое число). Чтобы включить эту многокритериальную сортировку непосредственно в класс Book, вы должны наделить его этой возможностью, реализовав интерфейс Comparable<Book>. Внутри метода compareTo реализуйте свою логику сортировки: сначала расставьте книги по publicationYear в возрастающем порядке (самые ранние первыми). Если две книги имеют одинаковый publicationYear, то только тогда сравните их bookTitle в алфавитном порядке.

Теперь давайте заполним ваш каталог для проверки. В вашем методе main создайте динамический список, способный хранить объекты Book. Добавьте в этот список три различные записи Book. Важно, чтобы у двух из этих книг был одинаковый publicationYear, но разные bookTitle, чтобы можно было протестировать ваше вторичное правило сортировки. Как только ваш список будет готов, примените магию сортировки. Наконец, пройдитесь по вашему идеально упорядоченному списку и отобразите bookTitle каждой книги, одну за другой, подтверждая безупречную организацию вашего каталога.

#### Требования:
1. Необходимо создать класс Book с двумя приватными полями: bookTitle (String) и publicationYear (int).
2. Поля bookTitle и publicationYear должны быть инкапсулированы (private), доступ к ним осуществляется через методы (геттеры или конструктор).
3. Класс Book должен реализовать интерфейс Comparable<Book>.
4. В классе Book необходимо реализовать метод compareTo так, чтобы книги сортировались сначала по publicationYear в порядке возрастания, а при совпадении года — по bookTitle в алфавитном порядке.
5. В методе main необходимо создать динамический список (например, ArrayList<Book>) для хранения объектов Book.
6. В список должны быть добавлены три объекта Book, причём у двух из них должен быть одинаковый publicationYear, но разные bookTitle.
7. Список книг должен быть отсортирован с помощью Collections.sort или list.sort().
8. После сортировки необходимо пройтись по списку и вывести bookTitle каждой книги в отдельной строке.