
### Управление уникальностью и ранжированием городов 🏙️

Вы — картограф и градостроитель, разрабатывающий сложную систему для управления данными о городах. Эта система должна распознавать по-настоящему уникальные города, а также иметь возможность ранжировать их по численности населения.

Во-первых, определите свой класс City. Каждый город будет характеризоваться своим cityName (строковое значение) и cityPopulation (целое число). Чтобы включить ранжирование по населению, ваш класс City должен реализовать интерфейс Comparable<City>. Внутри его метода compareTo укажите ему сравнивать города исключительно по их cityPopulation, располагая их от наименее населённых к наиболее населённым.

Но это ещё не всё! Чтобы ваша система по-настоящему понимала, что делает два города «одинаковыми», независимо от их разных мест в памяти, вы должны тщательно переопределить методы equals и hashCode. Объект City должен считаться равным другому только в том случае, если совпадают и его cityName, и cityPopulation. Это критически важно для коллекций, которые обеспечивают уникальность.

Теперь для практической демонстрации. В вашем методе main создайте два отдельных объекта City. Важно, чтобы вы присвоили обоим одинаковые значения cityName и cityPopulation. Затем создайте TreeSet — специальную коллекцию, которая автоматически сортирует свои элементы и отклоняет дубликаты, полагаясь на ваши реализации compareTo, equals и hashCode. Добавьте оба ваших, казалось бы, идентичных объекта City в этот TreeSet. Наконец, запросите у TreeSet его текущий размер и отобразите это число на экране. Если все ваши реализации Comparable, equals и hashCode находятся в идеальной гармонии, TreeSet должен уверенно сообщить, что он содержит только один уникальный город.

#### Требования:
1. Класс City должен содержать два поля: cityName (String) и cityPopulation (int).
2. Класс City должен реализовывать интерфейс Comparable<City>.
3. Метод compareTo класса City должен сравнивать города только по полю cityPopulation, сортируя их от наименьшего к наибольшему.
4. Метод equals класса City должен возвращать true только если оба объекта имеют одинаковые значения cityName и cityPopulation.
5. Метод hashCode класса City должен быть реализован так, чтобы для объектов с одинаковыми cityName и cityPopulation возвращалось одинаковое значение.
6. В методе main должны быть созданы два объекта City с одинаковыми значениями cityName и cityPopulation.
7. В методе main должен быть создан объект TreeSet<City> и в него должны быть добавлены оба созданных объекта City.
8. В методе main после добавления объектов City в TreeSet необходимо вывести размер TreeSet на экран.