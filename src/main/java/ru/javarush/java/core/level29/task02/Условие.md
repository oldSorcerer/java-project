
### Управление сотрудниками в новой HR-системе 👥

Вы — архитектор новой, высокоэффективной HR-системы, и одна из ваших ключевых задач — точно управлять записями о сотрудниках. В этой системе каждый сотрудник уникально идентифицируется не только своим именем, но и комбинацией имени и уникального идентификационного номера.

Начните с проектирования класса Employee. Этот класс должен инкапсулировать две ключевые части информации: employeeName (строковое значение) и employeeId (целое число). Задача состоит в том, чтобы ваша система распознавала два объекта Employee как представляющие одного и того же человека, если их значения employeeName и employeeId идентичны. Для этого вы должны тщательно переопределить методы equals и hashCode внутри вашего класса Employee, заставив их зависеть от обоих полей.

Теперь проверим интеллект вашей системы: создайте два абсолютно отдельных объекта Employee в памяти. Присвойте обоим объектам одинаковые значения employeeName и employeeId — например, оба могут быть "Алисой" с ID 123. Затем настройте HashMap, где объекты Employee будут служить ключами, а строковые значения будут представлять, скажем, их назначенную роль. Используя ваш первый созданный объект Employee, добавьте в этот HashMap пару, связав его со значением "Работник".

Момент истины наступает, когда вы пытаетесь получить информацию. Используйте ваш второй созданный объект Employee (тот, который отличается в памяти, но логически идентичен), чтобы попытаться извлечь связанную роль из вашего HashMap. Отобразите полученную роль на экране. Если ваши реализации equals и hashCode безупречны, ваша система должна триумфально вывести "Работник", доказывая свою способность распознавать одного и того же сотрудника независимо от используемого экземпляра объекта.

#### Требования:
1. Необходимо создать класс Employee с двумя приватными полями: employeeName (типа String) и employeeId (типа int).
2. В классе Employee должен быть корректно переопределён метод equals, который возвращает true, если оба поля (employeeName и employeeId) равны у сравниваемых объектов.
3. В классе Employee должен быть переопределён метод hashCode, который вычисляет хеш-код на основе обоих полей: employeeName и employeeId.
4. В основной части программы необходимо создать два независимых объекта Employee с одинаковыми значениями полей employeeName и employeeId.
5. Следует создать HashMap<Employee, String> и использовать первый объект Employee в качестве ключа при добавлении пары ключ-значение.
6. Необходимо попытаться получить значение из HashMap, используя второй объект Employee (с теми же значениями полей, но другой экземпляр), и вывести результат на экран.
7. В результате выполнения программы на экран должно быть выведено значение, связанное с первым объектом Employee, что подтверждает корректную реализацию equals и hashCode.