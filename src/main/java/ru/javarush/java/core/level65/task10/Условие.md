
### Тестирование геометрической библиотеки: проверка типов в record pattern 🧪

Представьте, что вы тестируете новую версию вашей геометрической библиотеки, и вам нужно убедиться в её надёжности, даже когда разработчики пытаются использовать её неправильно.

Ваша задача — сначала объявить простой record-класс Point, который будет содержать две целочисленные координаты: int x и int y.

Затем создайте переменную geometricObject типа Object и "положите" в неё экземпляр вашего Point.

Теперь, для проверки на прочность, специально попробуйте использовать сопоставление с образцом (pattern matching) таким образом, чтобы оно включало некорректные типы для компонентов Point при их распаковке, например, пытаясь извлечь String a и String b вместо ожидаемых int.

Попробуйте скомпилировать этот "ошибочный" код — вы должны увидеть, как умный компилятор укажет вам на несоответствие типов, предотвращая потенциальную ошибку ещё на этапе сборки.

После этого, исправьте ваш pattern matching так, чтобы он использовал правильные типы (int xCoord, int yCoord), и убедитесь, что код теперь компилируется и работает как надо, успешно распаковывая компоненты вашей Point.

#### Требования:
1. Должен быть объявлен record-класс Point с двумя компонентами типа int: x и y.
2. Необходимо создать переменную типа Object и присвоить ей экземпляр Point.
3. В задаче требуется попытаться выполнить сопоставление с образцом (pattern matching) для объекта типа Point, используя заведомо неверные типы для распаковки компонентов (например, String a, String b).
4. Код с некорректными типами pattern matching должен не компилироваться, и компилятор должен указывать на несоответствие типов.
5. После неудачной попытки необходимо исправить pattern matching, указав правильные типы (int xCoord, int yCoord) для распаковки компонентов record-класса Point.
6. После исправления кода pattern matching должен успешно компилироваться и корректно распаковывать значения x и y из объекта Point.