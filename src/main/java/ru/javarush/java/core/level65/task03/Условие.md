
### Интеллектуальный анализатор данных 🔍

Представьте, что вы создаёте умный анализатор данных, который способен быстро распознавать и сообщать, что перед ним: текст, число или что-то совершенно иное.

Ваша задача — написать метод printTypeInfo, который принимает на вход любой объект dataObject. Этот метод должен использовать элегантную возможность сопоставления с образцом (pattern matching) в операторе instanceof для определения типа.

Если dataObject окажется текстовой строкой, метод должен вежливо сообщить: "Это строка: " и далее вывести её содержимое. В случае, если dataObject представляет собой любое число (будь то Integer, Double или любое другое наследник Number), анализатор должен выдать: "Это число: " и затем отобразить само число. А если же перед ним будет что-то совершенно неожиданное, ваш анализатор должен честно признаться: "Неизвестный тип".

#### Требования:
1. Метод printTypeInfo должен использовать синтаксис pattern matching с оператором instanceof для определения типа объекта.
2. Если объект dataObject является строкой (String), метод должен вывести сообщение "Это строка: " и содержимое строки.
3. Если объект dataObject является экземпляром любого класса-наследника Number (например, Integer, Double и т.д.), метод должен вывести сообщение "Это число: " и значение числа.
4. Если объект dataObject не является ни строкой, ни наследником Number, метод должен вывести сообщение "Неизвестный тип".