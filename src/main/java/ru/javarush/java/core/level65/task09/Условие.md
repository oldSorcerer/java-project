
### Область видимости переменной при анализе данных 📊

Вы работаете над аналитической системой, которая обрабатывает входящие данные, и вам нужно понять, как далеко "видны" переменные, созданные с помощью нового синтаксиса.

Ваша задача — создать переменную unknownData типа Object и временно "поместить" в неё обычную текстовую строку.

Затем, внутри условного блока if, используйте элегантное сопоставление с образцом (pattern matching) в сочетании с instanceof String textValue, чтобы не только убедиться, что unknownData действительно является строкой, но и сразу получить доступ к ней через новую переменную textValue.

Если это строка, выведите на экран её длину, демонстрируя успешное извлечение.

А теперь самое интересное: попробуйте после этого блока if снова обратиться к переменной textValue и вывести её на экран. Этот эксперимент поможет вам чётко понять, где заканчивается "видимость" (область видимости) переменных, созданных таким образом.

#### Требования:
1. В программе должна быть объявлена переменная с именем unknownData типа Object.
2. Переменной unknownData должно быть присвоено значение обычной текстовой строки.
3. Внутри блока if необходимо использовать конструкцию pattern matching с instanceof для проверки, что unknownData является строкой, и одновременного объявления переменной textValue.
4. Если unknownData — строка, внутри блока if нужно вывести на экран длину строки, используя переменную textValue.
5. После завершения блока if необходимо попытаться обратиться к переменной textValue и вывести её на экран, чтобы продемонстрировать область видимости этой переменной.