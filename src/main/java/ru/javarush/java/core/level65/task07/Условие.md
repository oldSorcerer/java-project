
### Геометрический редактор: описание положения точек 📍

Представьте, что вы создаете интерактивный графический редактор, которому необходимо точно определять положение каждой точки на плоскости. Ваша задача — сначала объявить компактный record-класс Point, который будет хранить две координаты: int x и int y.

Затем напишите метод describePoint, принимающий на вход любой объект inputObject. Этот метод должен быть умным и использовать современный синтаксис switch с record patterns и элегантным guard-выражением (when) для детального описания точки.

Если точка окажется лежащей прямо на оси X (то есть её координата y равна 0), ваш редактор должен сообщить: "На оси X: x=<значение x>". Если же точка расположилась на оси Y (координата x равна 0), выведите: "На оси Y: y=<значение y>". В случае, когда это просто "обычная" точка, не лежащая ни на одной из осей, выведите её полные координаты: "Обычная точка: (<значение x>,<значение y>)". А если inputObject окажется чем-то совершенно посторонним, не точкой вовсе, ваш метод должен честно сказать: "Не точка".

Продемонстрируйте блестящую работу вашего метода, передавая ему поочередно Point(0, 5), Point(7, 0), Point(4, 6) и, для проверки, обычную строку "abc".

#### Требования:
1. Необходимо объявить record-класс с именем Point, содержащий два поля типа int: x и y.
2. Метод describePoint должен принимать на вход параметр типа Object (например, Object inputObject).
3. В методе describePoint должен использоваться современный switch с record patterns для распознавания объектов типа Point.
4. Внутри switch должны использоваться guard-выражения (when) для проверки значений координат x и y.
5. Если координата y равна нулю, метод должен выводить строку: "На оси X: x=<значение x>".
6. Если координата x равна нулю, метод должен выводить строку: "На оси Y: y=<значение y>".
7. Если точка не лежит ни на одной из осей, метод должен выводить строку: "Обычная точка: (<значение x>,<значение y>)".
8. Если inputObject не является объектом типа Point, метод должен выводить строку: "Не точка".
9. В программе должно быть продемонстрировано, что describePoint корректно работает для объектов Point(0, 5), Point(7, 0), Point(4, 6) и строки "abc".