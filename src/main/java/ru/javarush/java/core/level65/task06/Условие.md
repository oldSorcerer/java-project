
### Умная система идентификации питомцев для ветеринарной клиники 🐕🐈🦜

Вы работаете над умной системой для ветеринарной клиники, которая должна мгновенно распознавать и представлять информацию о каждом животном. Ваша задача — создать запечатанный (sealed) интерфейс Animal, который будет служить эталоном для всех питомцев. Этот интерфейс должен допускать лишь три конкретных, окончательных типа животных: Cat, Dog и Parrot. Каждый из этих классов должен быть final и содержать одно важное поле — String name, чтобы мы могли знать имя каждого уникального существа.

После определения этой строгой иерархии, создайте переменную currentPet типа Animal и "представьте", что к вам на приём привезли собаку по кличке "Rex", присвоив ей соответствующий объект Dog. Теперь, используя всю мощь сопоставления с образцом (pattern matching) внутри оператора switch, ваша система должна мгновенно определить, кто перед ней, и вывести на экран точное описание: для кошки — "Кот: <имя>", для собаки — "Собака: <имя>", и для попугая — "Попугай: <имя>".

#### Требования:
1. В коде должен быть объявлен sealed-интерфейс Animal, который ограничивает допустимые реализации.
2. Интерфейс Animal должен разрешать только три final-класса: Cat, Dog и Parrot.
3. Классы Cat, Dog и Parrot должны быть объявлены как final и реализовывать интерфейс Animal.
4. Каждый из классов Cat, Dog и Parrot должен содержать поле String name для хранения имени животного.
5. В коде должна быть создана переменная currentPet типа Animal.
6. Переменной currentPet должен быть присвоен объект Dog с именем "Rex".
7. Для определения типа животного и получения имени должно использоваться pattern matching в операторе switch по переменной currentPet.
8. В зависимости от типа животного, программа должна выводить "Кот: <имя>", "Собака: <имя>" или "Попугай: <имя>" соответственно.