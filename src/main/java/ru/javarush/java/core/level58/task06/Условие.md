
### Секретный Обмен: Операция "Мгновенный Курьер" 🕵️

Вы разрабатываете систему сверхсекретного обмена данными для двух агентов под кодовыми именами "Альфа" и "Бета". Они находятся в разных точках мира, и им нужно одновременно обменяться критически важными сообщениями. Ни один из агентов не может получить информацию, пока не отправит свою. Они словно жмут друг другу руки, обмениваясь важными сведениями в режиме реального времени.

Ваша миссия — написать программу, которая моделирует этот обмен. Создайте два отдельных потока, каждый из которых будет представлять одного агента. Агент "Альфа" должен подготовить секретный доклад "Сообщение от первого потока" и отправить его, ожидая ответ. В то же время, агент "Бета" должен отправить свой ответ "Ответ от второго потока", также ожидая сообщение от "Альфы". Используйте специальный механизм для синхронизированного обмена данными между потоками. По завершении обмена каждый агент должен раскрыть содержимое полученной секретной информации, выведя её на экран, чтобы убедиться, что передача прошла успешно.

#### Требования:
1. В программе должны быть созданы два отдельных потока, моделирующих агентов "Альфа" и "Бета".
2. Для обмена сообщениями между потоками должен быть использован высокоуровневый синхронизатор для двустороннего обмена (например, Exchanger из java.util.concurrent).
3. Первый поток ("Альфа") должен отправлять сообщение "Сообщение от первого потока", а второй поток ("Бета") — "Ответ от второго потока".
4. Каждый поток должен дождаться получения сообщения от другого агента до продолжения выполнения.
5. После обмена каждый поток должен вывести на экран содержимое полученного сообщения.
6. Программа должна завершаться только после того, как оба потока получат и выведут свои сообщения.