
### Умный Дом: Безопасная Публикация Конфигурации 🏡

Представьте, что вы создаете ядро системы "Умный дом". В этой системе есть центральный объект HomeConfiguration, который хранит жизненно важные настройки, например, securityCode (код безопасности). Этот объект сначала создается и инициализируется одним из внутренних модулей системы. После его полной готовности, он должен быть мгновенно и безопасно доступен для чтения другими частями "Умного дома", например, панелью управления, которая должна отображать текущий securityCode. Крайне важно, чтобы панель всегда видела только полностью сформированный и актуальный объект конфигурации, без каких-либо "незавершённых" состояний.

Ваша задача — создать класс HomeConfiguration с полем securityCode, которое будет инициализировано значением 123 при создании объекта. Затем объявите специальную ссылку volatileHomeConfig на HomeConfiguration таким образом, чтобы изменения, сделанные в одном потоке, были гарантированно видны в другом. В первом потоке создайте экземпляр HomeConfiguration и присвойте его этой специальной ссылке. Во втором потоке, как только ссылка volatileHomeConfig станет доступной (не будет равна null), панель управления должна немедленно считать и вывести на экран значение securityCode. Ожидаемый результат на экране — число 123.

#### Требования:
1. Необходимо создать класс с именем HomeConfiguration.
2. В классе HomeConfiguration должно быть поле securityCode, которое инициализируется значением 123 при создании объекта.
3. Ссылка volatileHomeConfig на объект HomeConfiguration должна быть объявлена с модификатором volatile для обеспечения безопасной публикации между потоками.
4. В первом потоке необходимо создать экземпляр HomeConfiguration и присвоить его volatileHomeConfig.
5. Во втором потоке необходимо дождаться, когда volatileHomeConfig станет отличной от null, после чего считать из неё значение securityCode и вывести его на экран.
6. Панель управления (второй поток) должна видеть только полностью инициализированный объект HomeConfiguration, без промежуточных или некорректных состояний.
7. В результате выполнения программа должна вывести на экран число 123.