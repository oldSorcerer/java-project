
### Центр Управления Полётами: Система Кэширования Высоты 🛰️

Представьте, что вы разрабатываете центральный узел данных для системы управления космическим кораблем. Одним из важнейших показателей является текущая "Орбитальная Высота" модуля. Это значение постоянно обновляется бортовыми системами, но в то же время его должны мгновенно читать множество приборов и экранов в Центре Управления Полётами. Вам нужно обеспечить, чтобы данные всегда были актуальными и доступными, при этом не допуская конфликтов при одновременной записи и чтении.

Создайте класс TelemetryCache, который будет хранить единственную переменную currentOrbitalAltitude (целое число) и использовать передовой механизм для контроля доступа к ней. Реализуйте метод updateAltitude(int newAltitude), который будет безопасно записывать новое значение высоты, убеждаясь, что в момент записи никто не пытается её прочитать. А также метод getAltitude(), который позволит множеству систем безопасно считывать текущую высоту, не мешая друг другу.

Инициируйте ваш TelemetryCache, установите начальную высоту, например, 123 единицы. Затем прочитайте это значение и выведите его на главный монитор. В итоге, на экране должно появиться число 123, подтверждая корректность работы вашей системы.

#### Требования:
1. Класс TelemetryCache должен использовать механизм синхронизации StampedLock для управления доступом к переменной currentOrbitalAltitude.
2. В классе TelemetryCache должна быть приватная переменная currentOrbitalAltitude типа int для хранения актуальной орбитальной высоты.
3. Метод updateAltitude(int newAltitude) должен использовать writeLock() StampedLock для эксклюзивной записи значения currentOrbitalAltitude, чтобы во время записи ни один поток не мог читать значение.
4. Метод getAltitude() должен использовать readLock() StampedLock для безопасного параллельного чтения значения currentOrbitalAltitude несколькими потоками одновременно.
5. В основной части программы (или демонстрации) необходимо создать объект TelemetryCache, установить начальное значение высоты равным 123 с помощью updateAltitude, затем считать его с помощью getAltitude и вывести на экран (System.out.println).
6. Реализация должна гарантировать отсутствие состояния гонки при одновременном доступе к currentOrbitalAltitude разными потоками на чтение и запись.