
### Сравнение числовых "контейнеров" 📦

Представьте, что вы разрабатываете специальный "умный" контейнер для чисел, который умеет автоматически сравнивать себя с другими такими же контейнерами. Это нужно для того, чтобы, например, сортировать их или находить наибольший/наименьший.

Создайте класс NumberBox, у которого будет одно целочисленное поле для хранения значения. Для этого класса реализуйте интерфейс Comparable<NumberBox>, чтобы он мог сравнивать себя с другими NumberBox'ами. Сравнение должно происходить по значению хранящегося внутри числа.

В основной части вашей программы создайте два объекта NumberBox с разными значениями (например, 10 и 5). Затем вызовите метод compareTo() на одном из них, передав другой, и выведите полученный результат на экран, чтобы увидеть, как они соотносятся.

#### Требования:
1. Должен быть создан публичный класс с именем NumberBox.
2. Класс NumberBox должен содержать приватное целочисленное поле для хранения значения.
3. Класс NumberBox должен реализовывать интерфейс Comparable<NumberBox>.
4. В классе NumberBox должен быть реализован публичный метод compareTo(NumberBox other), который сравнивает текущий объект с другим объектом NumberBox по значению их числовых полей.
5. Метод compareTo должен возвращать отрицательное число, если значение текущего объекта меньше значения переданного объекта; положительное число, если больше; и ноль, если значения равны.
6. В основной части программы должны быть созданы два объекта NumberBox с разными значениями (например, 10 и 5).
7. В основной части программы должен быть вызван метод compareTo на одном из объектов NumberBox, передав в качестве аргумента второй объект, а результат должен быть выведен на экран.