
### Голосовое объявление системы 📣

Представьте, что вы создаёте программу для автоматической системы объявлений, которая иногда должна произносить стандартные фразы без дополнительных входных данных.

Вам нужно реализовать простое действие: вывод на экран фиксированной строки, сообщающей о выполнении какой-то внутренней операции. Создайте для этого переменную, которая будет хранить ссылку на такое действие. Это действие должно быть реализовано с помощью лямбда-выражения, которое при своём вызове просто печатает фразу "Лямбда без параметров!". После того, как вы определите это действие, вызовите его, чтобы убедиться, что объявление прозвучало.

#### Требования:
1. Необходимо выбрать или объявить подходящий функциональный интерфейс для лямбда-выражения без параметров и возвращаемого значения (например, Runnable).
2. В программе должна быть объявлена переменная, которая будет хранить ссылку на лямбда-выражение.
3. Лямбда-выражение должно быть реализовано так, чтобы при вызове выводить на экран строку "Лямбда без параметров!".
4. После определения переменной с лямбда-выражением, необходимо вызвать это действие для вывода сообщения на экран.