
### Автоматическое закрытие ресурсов безопасности 🔑

Представьте, что вы работаете с очень важными и ограниченными системными ресурсами, например, с временными безопасными соединениями или файловыми потоками. Крайне важно, чтобы эти ресурсы всегда корректно закрывались после использования, даже если в процессе работы возникнут ошибки.

Создайте класс DemoResource, который будет имитировать такой ресурс. Для этого заставьте его реализовать интерфейс AutoCloseable. В методе close() класса DemoResource выведите на экран сообщение "Ресурс закрыт", чтобы отслеживать его освобождение.

В основной программе используйте конструкцию try-with-resources для работы с объектом DemoResource. Внутри блока try выведите сообщение "Работа с ресурсом", имитируя использование ресурса. Убедитесь, что после завершения блока try (будь то нормальное завершение или исключение), сообщение о закрытии ресурса появляется автоматически, подтверждая его надёжное освобождение.

#### Требования:
1. Класс DemoResource должен реализовывать интерфейс AutoCloseable.
2. В классе DemoResource должен быть переопределён публичный метод close(), который выводит на экран сообщение "Ресурс закрыт".
3. В основной программе объект DemoResource должен использоваться внутри конструкции try-with-resources.
4. Внутри блока try-with-resources должно выводиться сообщение "Работа с ресурсом".
5. После завершения блока try-with-resources (независимо от наличия исключения) метод close() должен вызываться автоматически, и сообщение "Ресурс закрыт" должно выводиться на экран.