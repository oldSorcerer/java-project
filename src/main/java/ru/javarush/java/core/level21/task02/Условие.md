
### Стандартизация пользовательского ввода 📝

Вы разрабатываете модуль обработки пользовательских данных для нового веб-сервиса. Ваша задача — убедиться, что любой текстовый ввод, прежде чем быть сохранённым или обработанным дальше, всегда преобразуется к верхнему регистру для унификации.

Определите переменную, которая будет представлять собой "обработчик текста". Этот обработчик должен принимать одну строку и выводить её на экран, но обязательно в **верхнем регистре**. Используйте лямбда-выражение для реализации логики этого обработчика. Чтобы проверить его работу, передайте в него строку "java" и посмотрите, как она будет преобразована и выведена.

#### Требования:
1. Должен быть определён функциональный интерфейс или использоваться существующий (например, Consumer<String>), который принимает одну строку и не возвращает значения.
2. Обработчик текста должен быть реализован с использованием лямбда-выражения.
3. Лямбда-выражение должно преобразовывать переданную строку к верхнему регистру перед выводом.
4. Обработчик обязан вывести преобразованную строку на экран.
5. Обработчик должен быть вызван с аргументом "java", и результатом на экране должна быть строка "JAVA".