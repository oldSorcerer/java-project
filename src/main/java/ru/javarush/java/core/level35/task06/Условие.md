
### Просмотр журнала событий системы 💻

Вы — системный администратор, и вам нужно быстро просмотреть содержимое файла "system_log.txt", который содержит важные записи о работе сервера. Этот файл может быть очень большим, но вам нужно видеть каждую строку в отдельности, чтобы анализировать события.

Создайте файл "system_log.txt" и запишите в него несколько строк с выдуманными событиями (например, "Сервер запущен", "Ошибка базы данных", "Пользователь вошел").

Ваша задача — написать программу, которая аккуратно прочитает все эти строки из файла и выведет их на экран, каждую на новой строке, чтобы вы могли легко проанализировать происходящее.

#### Требования:
1. Необходимо создать файл с именем "system_log.txt" и записать в него несколько строк, каждая из которых содержит описание выдуманного события.
2. Для записи и чтения файла должны использоваться стандартные средства работы с файлами в Java (например, классы FileWriter, FileReader, BufferedReader, Files и др.).
3. Программа должна прочитать содержимое файла "system_log.txt" построчно, чтобы каждое событие обрабатывалось отдельно.
4. Каждая считанная строка (событие) должна быть выведена на экран с новой строки, в том же порядке, в котором она записана в файле.
5. Все используемые ресурсы для чтения и записи файлов должны быть корректно закрыты после завершения операций.