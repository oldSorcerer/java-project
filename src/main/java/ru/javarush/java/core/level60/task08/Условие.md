
### Архитектор цифровой адресной книги 🗺️

Вам поручено стать главным архитектором и спроектировать модульную структуру для вашей новой, изящной «Цифровой Адресной Книги». Это приложение будет четко разделено на три фундаментальные части: ядро, где живет сама суть данных о контактах; слой данных, который отвечает за хранение и извлечение этих контактов; и пользовательский интерфейс, через который пользователи будут взаимодействовать с книгой. Ваша задача — не просто представить себе эту структуру, а воплотить ее в физических каталогах и файлах.

В результате ваших усилий должен получиться проект, где каждый модуль живет в своем собственном доме, имеет свой исходный код и свой module-info.java, который определяет его роль и связи с другими модулями. Каждый модуль будет иметь свое собственное пространство для исходного кода в стандартной иерархии src/main/java, где логически будут располагаться классы, отвечающие за его функциональность. Вы должны создать все необходимые директории и пустые module-info.java файлы, чтобы структура вашего проекта выглядела идеально и функционально.

#### Требования:
1. Проект должен быть разделен на три отдельных модуля: core (ядро), data (слой данных) и ui (пользовательский интерфейс).
2. Для каждого модуля должна быть создана отдельная директория на верхнем уровне проекта.
3. Внутри каждого модуля должна быть создана стандартная иерархия каталогов src/main/java для размещения исходных файлов.
4. В каждом модуле должен быть создан файл module-info.java, который определяет модуль и его зависимости (на данном этапе файл может быть пустым).
5. В каталоге src/main/java каждого модуля должны находиться только те классы, которые относятся к его функциональности (ядро — модели и бизнес-логика, data — хранение и извлечение, ui — взаимодействие с пользователем).
6. В корне проекта не должно быть исходных файлов Java вне модульных директорий.