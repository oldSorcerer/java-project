
### Минимальное сердце модуля 🫀

Представьте, что вы — архитектор, и вам нужно создать самую базовую, самодостаточную комнату в огромном цифровом здании. Эта комната, ваш первый модуль, пока не нуждается ни в чем извне и не собирается ничего открывать миру. Она существует как независимая сущность.

Ваша задача — создать файл с именем module-info.java для модуля, который вы назовете my.first.module. Внутри этого файла вам необходимо объявить модуль таким образом, чтобы он не экспортировал никаких своих внутренних компонентов наружу и не заявлял о зависимости ни от каких других модулей. В результате ваших усилий должен получиться идеально сформированный, корректный module-info.java, который провозглашает существование вашего модуля my.first.module в его самой минимальной, но функциональной форме.

```
module my.first.module {
}
```

#### Требования:
1. Файл должен называться module-info.java.
2. В файле module-info.java должно быть объявление модуля с именем my.first.module.
3. Модуль my.first.module не должен экспортировать ни одного пакета.
4. Модуль my.first.module не должен содержать ни одной директивы requires (не зависит от других модулей).
5. Содержимое файла module-info.java должно быть валидным с точки зрения синтаксиса Java модулей.