
### Мастерская цифровых инструментов и демонстрация их силы 🛠️

Вы — мастер-изобретатель, который создал универсальную цифровую мастерскую, названную toolkit. В этой мастерской вы разработали два типа полезных предметов: уникальные Tool в пакете com.example.tools и универсальные Util в пакете com.example.utils. Каждый из них умеет гордо называть свое имя. Ваша цель — сделать так, чтобы эти изобретения были доступны для использования в вашей демонстрационной комнате.

Сначала, в файле module-info.java вашей мастерской toolkit, вы должны торжественно объявить, что оба пакета, com.example.tools и com.example.utils, готовы быть представлены миру.

Затем вы создаете отдельный модуль demo, вашу демонстрационную комнату, которая официально заявляет, что ей нужны ресурсы из toolkit. Внутри demo вы создадите класс Main, который, как фокусник, вызовет к жизни по одному экземпляру Tool и Util. После этого Main должен попросить каждый из них назвать свое имя и вывести их на экран.

В конце представления зрители должны увидеть две строки: сначала "Tool", а затем "Util", подтверждая, что ваши изобретения успешно работают и доступны.

#### Требования:
1. В файле module-info.java модуля toolkit должны быть явно экспортированы пакеты com.example.tools и com.example.utils с помощью директивы exports.
2. В пакете com.example.tools должен быть создан публичный класс Tool, а в пакете com.example.utils — публичный класс Util.
3. Класс Tool должен содержать публичный метод, возвращающий строку "Tool", а класс Util — публичный метод, возвращающий строку "Util".
4. Модуль demo должен содержать файл module-info.java, в котором с помощью директивы requires объявлена зависимость от модуля toolkit.
5. Внутри модуля demo должен быть создан класс Main, который импортирует классы Tool и Util и создает их экземпляры.
6. В классе Main модуля demo необходимо вызвать методы, возвращающие имя, у экземпляров Tool и Util, и вывести их значения на экран в две строки: сначала "Tool", затем "Util".