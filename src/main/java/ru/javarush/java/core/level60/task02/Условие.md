
### Открытие шлюза к библиотеке 🔑

Вы — создатель потрясающей библиотеки com.example.library, наполненной множеством полезных, но порой слишком сложных внутренних механизмов. Ваша цель — предоставить другим разработчикам доступ только к тщательно отобранной, безопасной и документированной части вашей библиотеки – ее публичному API. Остальные детали должны оставаться скрытыми, чтобы никто случайно не сломал ваши внутренние механизмы.

Ваша миссия состоит в том, чтобы создать файл module-info.java для вашего модуля com.example.library. В этом файле вам нужно четко указать, что из всего богатства вашей библиотеки экспортируется наружу только один, очень специфический пакет: com.example.library.api. Таким образом, вы получите безупречно составленный module-info.java, который служит своего рода «шлюзом», предоставляя доступ исключительно к указанному пакету.

#### Требования:
1. В файле module-info.java должно быть объявлено имя модуля com.example.library.
2. В module-info.java должен быть экспортирован только пакет com.example.library.api.
3. Все остальные пакеты модуля, кроме com.example.library.api, не должны экспортироваться.
4. Файл module-info.java должен быть синтаксически корректным и соответствовать спецификации Java 9+.