
### Строительство крепости многомодульного проекта 🏰

Вы — главный строитель, которому поручено возвести грандиозную цифровую крепость с помощью Maven. Эта крепость не будет единым монолитным блоком, а будет состоять из двух взаимосвязанных, но отдельных башен: центральной core, где будут храниться все фундаментальные правила и механизмы, и главной башни app, которая станет сердцем всей программы и будет координировать действия.

Ваша миссия — тщательно спланировать и создать скелет этой крепости. Вам необходимо определить корневой каталог проекта, в котором будет лежать генеральный план (pom.xml файл), и затем внутри него возвести две отдельные башни — core и app. Каждая из этих башен должна быть готова к приему строительных материалов, имея внутри себя стандартную структуру для исходного кода Java, то есть папку src/main/java. Ваша задача — создать все необходимые директории и файлы, чтобы эта базовая структура была безупречна.

#### Требования:
1. В корневом каталоге проекта должен находиться файл pom.xml, который будет являться генеральным планом (parent POM) всего многомодульного проекта.
2. Внутри корневого каталога должны быть созданы две директории: core и app, каждая из которых будет отдельным Maven-модулем.
3. Внутри каждой директории core и app должна быть создана стандартная структура Maven для исходного кода: src/main/java.
4. В директориях core и app должен присутствовать свой pom.xml, описывающий соответствующий модуль.
5. Корневой pom.xml должен содержать секцию <modules>, в которой перечислены оба модуля: core и app.
6. Все директории и файлы должны соответствовать стандартам Maven для многомодульных проектов.
7. Структура src/main/java в каждом модуле должна быть пустой, но готовой для добавления исходных файлов Java.