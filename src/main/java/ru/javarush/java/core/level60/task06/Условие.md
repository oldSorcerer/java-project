
### Хранитель секретов сервиса 🤫

Вы — хранитель могущественного, но чувствительного бэкенд-сервиса. Он предлагает внешний «интерфейс» — ServiceAPI — который служит его публичным лицом, но его внутренние механизмы, скрытые в ServiceImpl, являются строго охраняемой тайной. Ваш клиентский модуль очень хочет использовать этот сервис, но он должен уважать границы и никогда не заглядывать в его внутреннее устройство.

Итак, в вашем модуле service вы создаете общедоступный интерфейс ServiceAPI в пакете com.example.service.api, объявляющий метод execute(), который представляет собой публичное действие сервиса. Затем, в глубоко спрятанном пакете com.example.service.impl, вы создаете класс ServiceImpl, который является реальной, тайной реализацией ServiceAPI, и его метод execute() будет выводить на экран сообщение "Service executed". В файле module-info.java вашего модуля service вы должны очень строго указать, что экспортируется только пакет com.example.service.api, оставляя пакет реализации полностью скрытым.

Теперь представьте, что вы — разработчик client модуля. Вы объявляете, что ваш client требует service для своей работы. В классе ClientMain вы попытаетесь напрямую создать объект ServiceImpl. Ваша программа не должна скомпилироваться — это будет доказательством того, что ваша инкапсуляция работает! Затем, чтобы client все же мог использовать сервис, в модуле service вы создадите специальный класс, который будет служить "фабрикой" — он будет содержать статический метод, способный создать объект ServiceImpl и вернуть его, но только как тип ServiceAPI. Это как передать клиенту закрытую коробку, не показывая, что внутри.

Используйте этот "фабричный" метод в вашем client модуле, чтобы получить экземпляр ServiceAPI и вызвать его метод execute(). В конечном итоге, на экране должна появиться заветная строка "Service executed", подтверждая, что ваш сервис был вызван правильно, при этом его внутренние секреты остались в безопасности.

#### Требования:
1. В модуле service должен быть создан публичный интерфейс ServiceAPI в пакете com.example.service.api с методом execute().
2. В пакете com.example.service.impl модуля service должен быть создан класс ServiceImpl, реализующий интерфейс ServiceAPI и определяющий метод execute(), который выводит "Service executed" на экран.
3. В файле module-info.java модуля service должен экспортироваться только пакет com.example.service.api; пакет com.example.service.impl не должен экспортироваться.
4. Попытка создать объект ServiceImpl напрямую из другого модуля (например, client) должна приводить к ошибке компиляции.
5. В модуле service должен быть создан публичный класс (например, ServiceFactory) с публичным статическим методом, возвращающим объект типа ServiceAPI. Этот метод должен создавать и возвращать экземпляр ServiceImpl, но только как ServiceAPI.
6. В модуле client должен быть использован фабричный метод для получения экземпляра ServiceAPI и вызова метода execute(), чтобы на экран вывелось "Service executed".
7. Модуль client должен объявлять зависимость на модуль service в своем module-info.java.