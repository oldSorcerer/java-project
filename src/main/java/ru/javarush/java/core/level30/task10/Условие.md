
### Формирование Списка Профилей Пользователей с Приоритетом Первой Записи 👤

Вы разрабатываете систему управления пользователями для нового онлайн-сервиса. У вас есть импортированный список пользователей, каждый из которых имеет имя и возраст.

Но произошла небольшая путаница, и некоторые имена повторяются с разными возрастами, поскольку данные были внесены из разных источников.

Ваша задача — создать "чистую" базу данных, где каждый пользователь представлен только один раз по имени. Если имя встречается несколько раз, нужно сохранить возраст только того пользователя, который был записан первым в списке, игнорируя последующие записи с тем же именем, чтобы сформировать окончательные профили.

Выведите получившуюся структуру данных на экран.

#### Требования:
1. Необходимо использовать Stream API для обработки исходного списка пользователей.
2. Если имя пользователя встречается несколько раз, должен сохраняться возраст только того пользователя, который встретился первым в исходном списке.
3. Все последующие записи с тем же именем должны быть проигнорированы при формировании итоговой базы данных.
4. В результате должен быть сформирован список (или другая подходящая коллекция) уникальных пользователей, где каждый пользователь представлен только один раз по имени.
5. Получившаяся коллекция уникальных профилей пользователей должна быть выведена на экран.