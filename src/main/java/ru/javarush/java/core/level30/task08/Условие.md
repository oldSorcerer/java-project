
### Отладка Потока Данных в Умном Доме 🏠

Вы инженер, который отлаживает сложную систему обработки данных от датчиков в умном доме. У вас есть список числовых показаний, таких как температура или уровень освещённости: 10, 15, 20, 25, 30.

Для анализа вам нужны только те показания, что делятся на 10 без остатка. Однако, прежде чем эти данные будут преобразованы в текстовый формат для записи в лог, вы хотите в реальном времени видеть, какие именно значения проходят через этот фильтр, чтобы убедиться, что всё работает корректно.

После проверки все прошедшие данные должны быть превращены в строки и собраны в отдельный отчётный список. Выведите получившийся список строк на экран.

#### Требования:
1. В потоке должны остаться только те числовые значения, которые делятся на 10 без остатка.
2. Метод peek должен быть использован для вывода на экран значений, прошедших фильтр, до их преобразования в строку.
3. После фильтрации и промежуточного вывода все оставшиеся числа должны быть преобразованы в строки.
4. Преобразованные в строки значения должны быть собраны в отдельный список.
5. Получившийся список строк должен быть выведен на экран.