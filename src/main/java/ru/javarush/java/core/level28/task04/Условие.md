
### Объединение сокровищ из разных сундуков 💰

Вы — легендарный искатель приключений, и вам удалось найти несколько запрятанных сундуков с сокровищами. Проблема в том, что каждое открытие сундука давало вам отдельный список драгоценных камней, и теперь у вас есть список, содержащий другие списки камней! Ваша задача — объединить все эти драгоценности в один большой, сияющий список.

Начните с создания основного списка java.util.List со списками драгоценных камней, которые вы нашли: [ [1, 2], [3, 4, 5], [6] ]. Представьте, что числа здесь — это количество драгоценных камней определённого типа.

Ваша миссия: создать новый, единый список, в который вы добавите абсолютно все камни из всех вложенных сундуков. После того как вы соберёте все драгоценности воедино, выведите этот новый список на экран, чтобы полюбоваться своим богатством. В результате должно получиться: [1, 2, 3, 4, 5, 6].

#### Требования:
1. В программе должен быть создан основной список типа List<List<Integer>>, содержащий три вложенных списка: [1, 2], [3, 4, 5], и [6].
2. Все элементы из вложенных списков должны быть добавлены в один новый список типа List<Integer>.
3. В итоговом объединённом списке порядок элементов должен соответствовать порядку их появления во вложенных списках (результат: [1, 2, 3, 4, 5, 6]).
4. После объединения всех элементов программа должна вывести итоговый список на экран.