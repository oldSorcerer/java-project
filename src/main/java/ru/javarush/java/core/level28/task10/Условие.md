
### Разработка умного кэша для игровых ассетов 🚀

Вы — ведущий разработчик новой захватывающей видеоигры. Для оптимизации производительности игры вам нужен умный кэш, который будет хранить только самые часто используемые игровые ассеты (например, текстуры или модели персонажей). При добавлении нового ассета, если кэш заполнен, он должен автоматически удалять наименее недавно использованный ассет, чтобы освободить место. Это классическая стратегия LRU (Least Recently Used).

Создайте класс GameAssetCache, который будет наследовать от LinkedHashMap<Integer, String>. Установите максимальный размер вашего кэша равным 2. В конструкторе GameAssetCache обязательно используйте порядок по доступу (опция accessOrder = true). Самое важное: переопределите метод removeEldestEntry. Этот метод должен возвращать true только в том случае, если текущий размер кэша превышает его максимальную вместимость (в нашем случае, 2), что и вызовет удаление самого старого элемента.

В вашем главном методе main() создайте экземпляр GameAssetCache. Добавьте в него несколько ассетов: сначала ассет с ID 1 и названием "one", затем ассет с ID 2 и названием "two". Затем "используйте" ассет с ID 1 (вызовите get(1)), чтобы он стал самым недавно использованным. Наконец, добавьте новый ассет с ID 3 и названием "three". Это должно привести к автоматическому удалению ассета, который был наименее недавно использован до этого момента.

В конце, используйте цикл for-each по keySet() вашего кэша, чтобы вывести на экран все ключи оставшихся ассетов. Результат должен продемонстрировать, что LRU-логика работает:

```
2
1
3
```

#### Требования:
1. Класс GameAssetCache должен наследовать от LinkedHashMap<Integer, String>.
2. В конструкторе GameAssetCache должен быть вызван конструктор суперкласса с опцией accessOrder = true для организации порядка по доступу.
3. В классе GameAssetCache должно быть реализовано ограничение максимального размера кэша, равное 2.
4. Метод removeEldestEntry должен быть переопределён так, чтобы возвращать true только если текущий размер кэша превышает 2.
5. В методе main() должен быть создан экземпляр GameAssetCache, добавлены ассеты с ключами 1 ("one") и 2 ("two"), затем должен быть вызван get(1) для обновления порядка использования.
6. После добавления ассета с ключом 3 ("three") должен автоматически удалиться наименее недавно использованный ассет.
7. Программа должна вывести на экран (через for-each по keySet()) все ключи оставшихся в кэше ассетов, отражая порядок их использования.