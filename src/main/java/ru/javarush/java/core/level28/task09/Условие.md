
### Создание списка "недавно просмотренных товаров" для интернет-магазина 🛍️

Вы — разработчик интерфейса для популярного интернет-магазина. Ваша цель — улучшить пользовательский опыт, добавив функцию "Недавно просмотренные товары", которая динамически меняет порядок, выводя самые последние просмотренные товары в конец списка.

Используйте java.util.LinkedHashMap<String, Integer>, но будьте внимательны: при её создании укажите параметр accessOrder как true. Это заставит карту переупорядочивать элементы при каждом обращении к ним.

Теперь добавьте несколько условных товаров с их идентификаторами: "a" → 1, "b" → 2, "c" → 3. Далее, имитируйте действие пользователя: обратитесь к товару с ключом "b" (используя метод get). Этот шаг должен "поднять" товар "b" в конец списка просмотренных.

В завершение, пройдитесь по всем ключам карты и выведите их на экран, чтобы показать, как изменился порядок отображения "недавно просмотренных". Вы должны увидеть, что "b" переместился:

```
a
c
b
```

#### Требования:
1. Необходимо создать объект LinkedHashMap<String, Integer> с параметром accessOrder, установленным в значение true.
2. В карту должны быть добавлены пары "a" -> 1, "b" -> 2, "c" -> 3.
3. После добавления элементов необходимо вызвать метод get("b") для карты.
4. После вызова get("b") элемент с ключом "b" должен переместиться в конец порядка обхода карты.
5. Необходимо пройтись по ключам карты и вывести их на экран, чтобы порядок был: a, c, b.