
### Управление агентами в онлайн-маркетплейсе 🛍️

Вы разрабатываете многозадачную серверную систему для оживленного онлайн-магазина. Вам нужны специализированные "агенты", которые будут параллельно обрабатывать разные типы входящих запросов.

Первый тип — это агенты по приветствию пользователей, назовите их UserGreeter. Каждый такой агент должен знать имя пользователя и при приветствии указывать, из какого "потока" он обращается, например: "Привет, [имя пользователя] из потока [имя потока]".

Второй тип — это агенты по обработке заказов, OrderHandler. Каждый из них должен получить номер заказа и сообщать: "Обрабатываю заказ [номер заказа] в потоке [имя потока]".

Ваша задача — создать двух агентов UserGreeter для разных пользователей и одного OrderHandler для конкретного заказа, а затем запустить их всех одновременно, чтобы каждый мог выполнять свою работу параллельно, не мешая другим.

#### Требования:
1. Необходимо создать два отдельных класса: UserGreeter и OrderHandler, реализующих работу в отдельных потоках.
2. Класс UserGreeter должен принимать имя пользователя, а OrderHandler — номер заказа. Эти параметры должны сохраняться в соответствующих полях объектов.
3. Каждый агент должен быть реализован как отдельный поток — либо через наследование от Thread, либо через реализацию интерфейса Runnable.
4. UserGreeter должен выводить сообщение "Привет, [имя пользователя] из потока [имя потока]", а OrderHandler — "Обрабатываю заказ [номер заказа] в потоке [имя потока]". Имя потока должно определяться динамически через Thread.currentThread().getName().
5. В основной части программы необходимо создать два потока UserGreeter с разными именами пользователей и один OrderHandler с конкретным номером заказа. Все потоки должны быть запущены одновременно с помощью метода start().
6. Все агенты должны работать параллельно, то есть их сообщения могут выводиться в любом порядке, в зависимости от планирования потоков.
7. Для наглядности можно явно задать имена потокам при их создании, чтобы в выводе отображались осмысленные имена потоков.