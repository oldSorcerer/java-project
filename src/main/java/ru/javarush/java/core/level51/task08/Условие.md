
### Координация роботов на умной фабрике 🏭

Вы – главный по задачам для команды прилежных роботов на умной фабрике. Каждый робот, назовем его TaskRunner, умеет выполнять одну конкретную задачу, которая занимает определенное время.

При запуске робот должен сообщить: "[имя потока]: старт задачи [название задачи] (приоритет X)", затем "заснуть" на заданное количество миллисекунд, имитируя выполнение задачи, и после пробуждения объявить: "[имя потока]: завершена задача [название задачи]".

Ваша цель — запустить трех таких роботов. Каждому роботу дайте уникальное название задачи, разное время выполнения и, что самое главное, установите им разные уровни важности: один робот должен работать с минимальным приоритетом, другой — с обычным, а третий — с максимальным. Убедитесь, что все роботы начнут свою работу.

#### Требования:
1. Необходимо создать класс TaskRunner, реализующий интерфейс Runnable, который будет представлять робота-исполнителя задачи.
2. Класс TaskRunner должен принимать в конструкторе название задачи и время выполнения задачи в миллисекундах.
3. В начале работы метода run() TaskRunner должен вывести сообщение в формате: "[имя потока]: старт задачи [название задачи] (приоритет X)", где X — приоритет потока.
4. В методе run() TaskRunner должен приостанавливать выполнение на заданное количество миллисекунд с помощью Thread.sleep.
5. После завершения «выполнения» задачи TaskRunner должен вывести сообщение в формате: "[имя потока]: завершена задача [название задачи]".
6. В основной программе необходимо создать три потока, каждый из которых запускает своего TaskRunner с уникальным названием задачи и разным временем выполнения.
7. Перед запуском каждому потоку необходимо установить уникальный приоритет: одному — минимальный (Thread.MIN_PRIORITY), второму — обычный (Thread.NORM_PRIORITY), третьему — максимальный (Thread.MAX_PRIORITY).
8. Все три потока должны быть запущены, чтобы каждый из роботов начал выполнение своей задачи.