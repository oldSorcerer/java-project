
### Поручение задачи новому стажёру 🧑‍💻

Вы — руководитель проекта, и вам нужно поручить новому стажёру, назовем его TaskExecutor, выполнять одну очень важную, но повторяющуюся задачу: информировать всех, что "Runnable is running!".

Этот стажёр не является "главным менеджером" (потоком), но он отлично умеет выполнять инструкции. Ваша задача — написать чёткие инструкции для TaskExecutor, оформив их как отдельный план действий.

Затем вам нужно "нанять" основного "рабочего" (поток), передать ему этот план TaskExecutor и приказать начать выполнять задачу.

В результате выполнения этой команды должно прозвучать "Runnable is running!" от нового, отдельного "потока" исполнения.

#### Требования:
1. Необходимо создать отдельный класс с именем TaskExecutor, который реализует интерфейс Runnable.
2. В классе TaskExecutor должен быть переопределён публичный метод void run(), в котором реализована логика задачи.
3. В методе run класса TaskExecutor должно быть реализовано действие по выводу на экран строки "Runnable is running!".
4. В основной программе (например, в методе main) необходимо создать новый объект Thread, передав в его конструктор объект TaskExecutor.
5. В основной программе нужно вызвать метод start() у созданного потока для начала выполнения задачи в отдельном потоке.