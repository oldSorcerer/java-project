
### Магия изменения скрытых чисел 🎩

Вы — мастер-иллюзионист, который демонстрирует трюк с "волшебной коробкой" (MagicBox, ваш класс SecretHolder). Внутри коробки скрыто число, и есть публичный способ узнать, какое число коробка "думает", что в ней находится. Ваша цель — с помощью "тайного заклинания" (рефлексии) незаметно изменить это спрятанное число, чтобы удивить зрителей.

Создайте класс SecretHolder с приватным полем private int secretValue и публичным методом public int revealSecret(), который возвращает secretValue.

В главной части программы (main) сначала получите и выведите на экран значение, которое MagicBox сообщает до вашего вмешательства. Затем, используя рефлексию и активируя "тайное заклинание" (setAccessible(true)), измените значение приватного поля secretValue на новое, например, на 99. После этого вновь получите и выведите на экран значение, которое MagicBox теперь сообщает через свой публичный метод. Зрители должны увидеть, как число изменилось "само по себе", благодаря вашей иллюзии.

#### Требования:
1. Необходимо создать класс с именем SecretHolder.
2. В классе SecretHolder должно быть приватное поле int с именем secretValue.
3. В классе SecretHolder должен быть публичный метод int revealSecret(), возвращающий значение поля secretValue.
4. В методе main нужно создать экземпляр класса SecretHolder.
5. В методе main необходимо вывести на экран значение, возвращаемое методом revealSecret() до изменения поля через рефлексию.
6. В методе main с помощью рефлексии и метода setAccessible(true) нужно изменить значение приватного поля secretValue у объекта SecretHolder.
7. Новое значение приватного поля secretValue должно быть равно 99.
8. В методе main необходимо вывести на экран значение, возвращаемое методом revealSecret() после изменения поля через рефлексию.