
### Автоматическая сборка роботов с умными компонентами 🤖

Представьте, что вы управляете высокотехнологичной фабрикой по производству роботов. Ваши роботы (Car класс) нуждаются в мощных источниках энергии (Engine класс), но вместо того, чтобы вручную подключать двигатель к каждому роботу, вы хотите, чтобы сборочная линия делала это автоматически. Роботы, которым нужен двигатель, имеют специальный "порт для подключения" (аннотацию @Inject) прямо на месте, где должен быть установлен двигатель.

Создайте аннотацию @Inject с жизненным циклом RUNTIME и возможностью применения к полям.

Затем создайте два класса: Engine (обычный класс) и Car.

В классе Car объявите приватное поле Engine, помеченное вашей аннотацией @Inject, и добавьте публичный конструктор без параметров.

Ваша задача — реализовать умный метод, который сможет создать объект Car. Затем, используя рефлексию, этот метод должен проверить все поля Car: если какое-то поле помечено аннотацией @Inject, система должна автоматически создать новый объект Engine и "внедрить" его в это поле. Это обеспечивает, что каждый робот получит свой источник энергии без ручного вмешательства.

#### Требования:
1. Необходимо объявить собственную аннотацию @Inject с retention policy RUNTIME и возможностью применения к полям (ElementType.FIELD).
2. Должен быть реализован обычный класс Engine без дополнительных требований.
3. В классе Car должно быть приватное поле типа Engine, помеченное аннотацией @Inject.
4. В классе Car должен присутствовать публичный конструктор без параметров.
5. Должен быть реализован отдельный метод, который создает экземпляр Car.
6. Метод автоматической сборки должен с помощью рефлексии находить все поля класса Car, помеченные аннотацией @Inject.
7. Если поле помечено @Inject, метод должен создать новый объект Engine и присвоить его этому полю с помощью рефлексии.
8. Метод должен корректно внедрять объект Engine даже в приватные поля класса Car.