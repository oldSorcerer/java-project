
### Шпионский перехват сообщений в дешифровщике 🕵️

Вы — секретный агент, которому поручено разработать систему безопасной связи. У вас есть базовый интерфейс для дешифровки сообщений (DecryptionUnit, ваш Calculator интерфейс) с ключевым методом decodeMessage (ваш метод add, который складывает две части сообщения). Также есть реальная реализация этого дешифровщика (DecryptionUnitImpl, ваш CalculatorImpl класс).

Ваша сверхсекретная задача — создать невидимый "слой перехвата" (динамический прокси) для этого дешифровщика. Этот слой должен работать так: каждый раз, когда кто-то пытается использовать метод decodeMessage, ваш "перехватчик" должен сначала незаметно вывести на экран сообщение "Метод decodeMessage вызван с частями: [часть A], [часть B]", показывая, какие части сообщения были переданы для дешифровки. И только после записи этой информации он должен позволить реальному дешифровщику выполнить свою работу и вернуть результат.

Создайте интерфейс DecryptionUnit с методом int decodeMessage(int partA, int partB).
Реализуйте класс DecryptionUnitImpl, который честно выполняет сложение.
Напишите код, который создает динамический прокси для DecryptionUnit. Убедитесь, что при вызове decodeMessage через прокси, сначала выводится лог, а затем возвращается результат от реального DecryptionUnitImpl.

#### Требования:
1. Должен быть объявлен интерфейс DecryptionUnit с методом int decodeMessage(int partA, int partB).
2. Должен быть реализован класс DecryptionUnitImpl, который реализует интерфейс DecryptionUnit и корректно складывает два числа, возвращая их сумму.
3. Должен быть создан динамический прокси для интерфейса DecryptionUnit с использованием механизма java.lang.reflect.Proxy.
4. При вызове метода decodeMessage через прокси, на экран должно выводиться сообщение "Метод decodeMessage вызван с частями: [часть A], [часть B]", где [часть A] и [часть B] — это реальные переданные аргументы.
5. После вывода сообщения прокси должен передать вызов реальному объекту DecryptionUnitImpl и вернуть его результат.